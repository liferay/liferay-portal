---
description: Export and triage Aikido findings for this project
---

# Aikido Analyse

Read the `aikido-analyse` skill and run its workflow for this project using `$ARGUMENTS` as the analysis scope.

Follow the skill's safety rules exactly: derive the Aikido repository id live, export findings, check `.aikido-ignore` drift, triage by `group_id`, and never apply dashboard ignores automatically.
