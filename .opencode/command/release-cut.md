---
description: Cut or prepare a release using the shared release-cut skill when available
---

# Release Cut

Read the `release-cut` skill and run its workflow for this project using `$ARGUMENTS` as the release scope.

If the `release-cut` skill is not available in the active OpenCode skill paths, stop and report the missing shared skill. Do not copy or fork the skill into `.opencode/skills/` or `~/.config/opencode/skills/`; install or expose the shared skill source instead.
