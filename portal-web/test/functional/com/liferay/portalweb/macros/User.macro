definition {

	var password = PropsUtil.get("default.admin.password");
	var userEmailAddress = "test@liferay.com";

	@summary = "Default summary"
	macro _clickSignInButton(localization = null) {
		var localizedSignIn = User._getSignInText(localization = ${localization});

		AssertClick(
			buttonId = "LoginPortlet",
			locator1 = "Button#BUTTON_ID",
			value1 = ${localizedSignIn});
	}

	@summary = "Default summary"
	macro _clickSignInLink(localization = null, idpName = null, samlMultiIdPs = null) {
		var localizedSignIn = User._getSignInText(localization = ${localization});

		if (IsElementNotPresent(locator1 = "TextInput#EMAIL_ADDRESS")) {
			if (isSet(localization)) {
				AssertClick(
					locator1 = "UserBar#USER_SIGN_IN",
					value1 = ${localizedSignIn});
			}
			else if (IsElementPresent(locator1 = "UserBar#USER_SIGN_IN")) {
				AssertClick(
					locator1 = "UserBar#USER_SIGN_IN",
					value1 = "Sign In");

				if (${samlMultiIdPs} == "true") {
					Select(
						key_fieldLabel = "Identity Provider",
						locator1 = "Select#GENERIC_SELECT_FIELD",
						value1 = ${idpName});

					AssertClick(
						buttonId = "LoginPortlet",
						key_text = "Sign In",
						locator1 = "Button#ANY_WITH_SPAN",
						value1 = "Sign In");
				}
			}
			else if (IsElementPresent(key_text = "Sign In", locator1 = "Button#ANY")) {
				Button.click(button = "Sign In");
			}
		}
	}

	@summary = "Default summary"
	macro _editUserInformation(userJobTitle = null, userComment = null, userTag = null, userLastName = null, userScreenName = null, userMiddleName = null, userEmailAddress = null, userCustomFieldLabel = null, userCustomField = null, userGender = null, userFirstName = null, userBirthday = null) {
		if (isSet(userScreenName)) {
			Type(
				locator1 = "UsersAndOrganizationsEditUser#DETAILS_SCREEN_NAME_FIELD",
				value1 = ${userScreenName});
		}

		if (isSet(userEmailAddress)) {
			Type(
				locator1 = "UsersAndOrganizationsEditUser#DETAILS_EMAIL_ADDRESS_FIELD",
				value1 = ${userEmailAddress});
		}

		if (isSet(userFirstName)) {
			Type(
				locator1 = "UsersAndOrganizationsEditUser#DETAILS_FIRST_NAME_FIELD",
				value1 = ${userFirstName});
		}

		if (isSet(userLastName)) {
			Type(
				locator1 = "UsersAndOrganizationsEditUser#DETAILS_LAST_NAME_FIELD",
				value1 = ${userLastName});
		}

		if (isSet(userBirthday)) {
			Type(
				locator1 = "UsersAndOrganizationsEditUser#DETAILS_BIRTHDAY_FIELD",
				value1 = ${userBirthday});
		}

		if (isSet(userMiddleName)) {
			Type(
				locator1 = "UsersAndOrganizationsEditUser#DETAILS_MIDDLE_NAME_FIELD",
				value1 = ${userMiddleName});
		}

		if (isSet(userJobTitle)) {
			Type(
				locator1 = "UsersAndOrganizationsEditUser#DETAILS_JOB_TITLE_FIELD",
				value1 = ${userJobTitle});
		}

		if (isSet(userGender)) {
			Select(
				locator1 = "UsersAndOrganizationsEditUser#DETAILS_GENDER_SELECT",
				value1 = ${userGender});
		}

		if (isSet(userTag)) {
			Type(
				locator1 = "UsersAndOrganizationsEditUser#DETAILS_TAG_FIELD_INPUT",
				value1 = ${userTag});

			KeyPress(
				locator1 = "UsersAndOrganizationsEditUser#DETAILS_TAG_FIELD_INPUT",
				value1 = "\ENTER");
		}

		if (isSet(userComment)) {
			Type(
				locator1 = "TextArea#COMMENTS",
				value1 = ${userComment});
		}

		if (isSet(userCustomField)) {
			Type(
				key_fieldLabel = ${userCustomFieldLabel},
				locator1 = "TextInput#GENERIC_TEXT_INPUT",
				value1 = ${userCustomField});
		}
	}

	@summary = "Default summary"
	macro _getSignInText(localization = null) {
		if (${localization} == "JA") {
			var localizedSignIn = "ログイン";
		}
		else if (${localization} == "ZH") {
			var localizedSignIn = "登录";
		}
		else if (${localization} == "IT") {
			var localizedSignIn = "Accedi";
		}
		else if (${localization} == "ES") {
			var localizedSignIn = "Acceder";
		}
		else {
			var localizedSignIn = "Sign In";
		}

		return ${localizedSignIn};
	}

	@summary = "Default summary"
	macro _login(userPassword = null, userEmailAddress = null) {
		SignIn._signIn(
			userEmailAddress = ${userEmailAddress},
			userPassword = ${userPassword});
	}

	@summary = "Default summary"
	macro acceptEndUserLicenseAgreement() {
		if (IsElementPresent(locator1 = "Button#I_AGREE")) {
			AssertClick(
				locator1 = "Button#I_AGREE",
				value1 = "I Agree");

			AssertElementNotPresent(locator1 = "Button#I_AGREE");
		}
	}

	@summary = "Default summary"
	macro acceptEndUserLicenseAgreementLocalized(localizedAgree = null) {
		var key_localizedAgree = ${localizedAgree};

		if (IsElementPresent(locator1 = "Button#I_AGREE_LOCALIZED")) {
			AssertClick(
				locator1 = "Button#I_AGREE_LOCALIZED",
				value1 = ${localizedAgree});

			AssertElementNotPresent(locator1 = "Button#I_AGREE_LOCALIZED");
		}
	}

	@summary = "Default summary"
	macro activateCP(userScreenName = null, userName = null) {
		ManagementBar.setFilterAndOrder(filterBy = "Inactive");

		LexiconEntry.gotoEntryMenuItem(
			menuItem = "Activate",
			rowEntry = ${userScreenName});

		Alert.viewSuccessMessage();

		ApplicationsMenu.gotoPortlet(
			category = "Users",
			panel = "Control Panel",
			portlet = "Users and Organizations");

		var key_userScreenName = ${userScreenName};

		AssertTextEquals(
			locator1 = "UsersAndOrganizations#USER_TABLE_NAME",
			value1 = ${userName});

		AssertTextEquals(
			locator1 = "UsersAndOrganizations#USER_TABLE_SCREEN_NAME",
			value1 = ${userScreenName});
	}

	@summary = "Default summary"
	macro addAddressViaMyAccount(userAddressCity = null, userAddressStreet1 = null, userAddressCountry = null, userAddressType = null, userAddressRegion = null, userAddressPostalCode = null) {
		Click(locator1 = "NavBar#ADDRESSES");

		Click(locator1 = "Button#ADD_ADDRESS");

		Type(
			locator1 = "TextInput#STREET_1",
			value1 = ${userAddressStreet1});

		if (isSet(userAddressCountry)) {
			Select(
				locator1 = "Select#COUNTRY",
				value1 = ${userAddressCountry});
		}

		if (isSet(userAddressRegion)) {
			Select(
				locator1 = "Select#NEW_REGION",
				value1 = ${userAddressRegion});
		}

		if (isSet(userAddressPostalCode)) {
			Type(
				locator1 = "TextInput#POSTAL_CODE",
				value1 = ${userAddressPostalCode});
		}

		if (isSet(userAddressType)) {
			Select(
				locator1 = "Select#TYPE",
				value1 = ${userAddressType});
		}

		Type(
			locator1 = "TextInput#CITY",
			value1 = ${userAddressCity});

		Check(locator1 = "Checkbox#MAKE_PRIMARY");

		Check(locator1 = "Checkbox#MAILING");

		Click(locator1 = "Button#SAVE");

		Alert.viewSuccessMessage();

		SelectFrame(value1 = "relative=top");
	}

	@summary = "Default summary"
	macro addContactInformation(country = null, number = null, skype = null, extension = null, address = null, city = null, postalCode = null, contactType = null, street1 = null, region = null, makePrimary = null, url = null) {
		if (isSet(contactType)) {
			var key_contactType = ${contactType};

			Click.mouseDownMouseUp(locator1 = "UsersAndOrganizationsEditUser#CONTACT_ADD_BUTTON");

			if (${makePrimary} == "true") {
				Check(
					checkboxName = "Make Primary",
					locator1 = "Checkbox#ANY_CHECKBOX");
			}

			// Addresses

			if (isSet(street1)) {
				Type(
					locator1 = "TextInput#STREET_1",
					value1 = ${street1});
			}

			if (isSet(city)) {
				Type(
					locator1 = "TextInput#CITY",
					value1 = ${city});
			}

			if (isSet(country)) {
				Select(
					locator1 = "Select#COUNTRY",
					value1 = ${country});
			}

			if (isSet(region)) {
				Select(
					locator1 = "Select#NEW_REGION",
					value1 = ${region});
			}

			if (isSet(postalCode)) {
				Type(
					locator1 = "TextInput#POSTAL_CODE",
					value1 = ${postalCode});
			}

			// Phone Numbers

			if (isSet(number)) {
				Type(
					locator1 = "TextInput#NUMBER",
					value1 = ${number});
			}

			if (isSet(extension)) {
				Type(
					locator1 = "TextInput#EXTENSION",
					value1 = ${extension});
			}

			// Additional Email Addresses

			if (isSet(address)) {
				Type(
					locator1 = "TextInput#ADDRESS",
					value1 = ${address});
			}

			// Websites

			if (isSet(url)) {
				Type(
					locator1 = "TextInput#URL",
					value1 = ${url});
			}
		}

		// Instant Messenger

		if (isSet(skype)) {
			Type(
				locator1 = "TextInput#SKYPE",
				value1 = ${skype});
		}

		Click(locator1 = "Button#SAVE");

		Alert.viewSuccessMessage();
	}

	@summary = "Default summary"
	macro addCP(userEmailAddressVariation = null, lng = null, userFirstName = null, userJobTitle = null, userComment = null, userTag = null, userScreenNameVariation = null, userLastName = null, userMiddleName = null, userGender = null, lat = null, userBirthday = null) {
		LexiconEntry.gotoAdd();

		User._editUserInformation(
			userBirthday = ${userBirthday},
			userComment = ${userComment},
			userEmailAddress = ${userEmailAddress},
			userFirstName = ${userFirstName},
			userGender = ${userGender},
			userJobTitle = ${userJobTitle},
			userLastName = ${userLastName},
			userMiddleName = ${userMiddleName},
			userScreenName = ${userScreenName},
			userTag = ${userTag});

		if (isSet(lat) && isSet(lng)) {
			Geolocation.setMarkerPosition(
				lat = ${lat},
				lng = ${lng});
		}

		var browserType = PropsUtil.get("browser.type");

		if (${browserType} == "safari") {
			Button.clickSave();
		}
		else {
			AssertClick(
				locator1 = "Button#SUBMIT",
				value1 = "Save");
		}

		if (${userEmailAddressVariation} == "Invalid") {
			AssertTextEquals(
				locator1 = "Message#ERROR_ENTER_A_VALID_EMAIL_ADDRESS",
				value1 = "Please enter a valid email address.");
		}
		else if (${userEmailAddressVariation} == "Null") {
			AssertTextEquals.assertPartialText(
				locator1 = "TextInput#REQUIRED_ALERT",
				value1 = "The Email Address field is required.");
		}
		else if (${userEmailAddressVariation} == "Root") {
			Alert.viewRequestFailedToComplete();

			AssertTextEquals.assertPartialText(
				locator1 = "Message#ERROR_ENTER_A_VALID_EMAIL_ADDRESS",
				value1 = "Please enter a valid email address.");
		}
		else if (${userScreenNameVariation} == "Invalid") {
			AssertTextEquals(
				locator1 = "Message#ERROR_FORM_VALIDATION",
				value1 = "The screen name cannot be an email address or a reserved word, such as postfix. It must contain only alphanumeric or the following special characters: -._.");
		}
		else {
			var userEmailAddress = StringUtil.lowerCase(${userEmailAddress});
			var userScreenName = StringUtil.lowerCase(${userScreenName});

			User._editUserInformation(
				userComment = ${userComment},
				userEmailAddress = ${userEmailAddress},
				userFirstName = ${userFirstName},
				userJobTitle = ${userJobTitle},
				userLastName = ${userLastName},
				userMiddleName = ${userMiddleName},
				userScreenName = ${userScreenName},
				userTag = ${userTag});
		}

		WaitForLiferayEvent.initializeLiferayEventLog();
	}

	@summary = "Default summary"
	macro addMultipleCP(userEmailAddressVariation = null, userScreenNameVariation = null, usersEmailsList = null, userMiddleName = null, userGender = null, userBirthday = null) {
		for (var userEmail : list ${usersEmailsList}) {
			var userEmailAddress = ${userEmail};
			var userName = StringUtil.replace(${userEmail}, "@liferay.com", "");

			ApplicationsMenu.gotoPortlet(
				category = "Users",
				panel = "Control Panel",
				portlet = "Users and Organizations");

			User.addCP(
				userBirthday = ${userBirthday},
				userEmailAddress = ${userEmailAddress},
				userEmailAddressVariation = ${userEmailAddressVariation},
				userFirstName = ${userName},
				userGender = ${userGender},
				userLastName = ${userName},
				userMiddleName = ${userMiddleName},
				userScreenName = ${userName},
				userScreenNameVariation = ${userScreenNameVariation});
		}
	}

	@summary = "Default summary"
	macro addPersonalSiteCP(personalSite = null, userScreenName = null, siteTemplateName = null) {
		UserNavigator.gotoUser(userScreenName = ${userScreenName});

		UserNavigator.gotoProfileAndDashboard();

		if (${personalSite} == "Public") {
			AssertTextEquals(
				key_subtitle = "Profile",
				locator1 = "UsersAndOrganizationsEditUser#PERSONAL_SITE_LABEL",
				value1 = "PROFILE");

			Select(
				locator1 = "UsersAndOrganizationsEditUser#PERSONAL_SITE_MY_PROFILE",
				value1 = ${siteTemplateName});
		}
		else {
			AssertTextEquals(
				key_subtitle = "Dashboard",
				locator1 = "UsersAndOrganizationsEditUser#PERSONAL_SITE_LABEL",
				value1 = "DASHBOARD");

			Select(
				locator1 = "UsersAndOrganizationsEditUser#PERSONAL_SITE_MY_DASHBOARD",
				value1 = ${siteTemplateName});
		}

		PortletEntry.save();
	}

	@summary = "Default summary"
	macro addReservedEmailAddressCP(userLastName = null, userScreenName = null, userEmailAddress = null, userFirstName = null) {
		LexiconEntry.gotoAdd();

		Type(
			locator1 = "UsersAndOrganizationsEditUser#DETAILS_SCREEN_NAME_FIELD",
			value1 = ${userScreenName});

		Type(
			locator1 = "UsersAndOrganizationsEditUser#DETAILS_EMAIL_ADDRESS_FIELD",
			value1 = ${userEmailAddress});

		Type(
			locator1 = "UsersAndOrganizationsEditUser#DETAILS_FIRST_NAME_FIELD",
			value1 = ${userFirstName});

		Type(
			locator1 = "UsersAndOrganizationsEditUser#DETAILS_LAST_NAME_FIELD",
			value1 = ${userLastName});

		Button.clickSave();

		Alert.viewErrorMessage(errorMessage = "The email address you requested is reserved.");
	}

	@summary = "Default summary"
	macro addReservedScreenNameCP(userLastName = null, userScreenName = null, userEmailAddress = null, userFirstName = null) {
		LexiconEntry.gotoAdd();

		Type(
			locator1 = "UsersAndOrganizationsEditUser#DETAILS_SCREEN_NAME_FIELD",
			value1 = ${userScreenName});

		Type(
			locator1 = "UsersAndOrganizationsEditUser#DETAILS_EMAIL_ADDRESS_FIELD",
			value1 = ${userEmailAddress});

		Type(
			locator1 = "UsersAndOrganizationsEditUser#DETAILS_FIRST_NAME_FIELD",
			value1 = ${userFirstName});

		Type(
			locator1 = "UsersAndOrganizationsEditUser#DETAILS_LAST_NAME_FIELD",
			value1 = ${userLastName});

		Button.clickSave();

		AssertTextEquals.assertPartialText(
			locator1 = "Message#ERROR_1",
			value1 = "The screen name you requested is reserved.");
	}

	@summary = "Default summary"
	macro advancedSearchViaFriendsDirectoryPG(searchTerm = null, searchField = null, searchNoResults = null, userLastName = null, userScreenName = null, userFirstName = null) {
		Click(locator1 = "NavBar#SEARCH_TOGGLE_ADVANCED");

		if (${searchField} == "First Name") {
			Type(
				locator1 = "TextInput#FIRST_NAME",
				value1 = ${searchTerm});
		}
		else if (${searchField} == "Middle Name") {
			Type(
				locator1 = "TextInput#MIDDLE_NAME",
				value1 = ${searchTerm});
		}
		else if (${searchField} == "Last Name") {
			Type(
				locator1 = "TextInput#LAST_NAME",
				value1 = ${searchTerm});
		}
		else if (${searchField} == "Screen Name") {
			Type(
				locator1 = "TextInput#SCREEN_NAME",
				value1 = ${searchTerm});
		}
		else if (${searchField} == "Email Address") {
			Type(
				locator1 = "UsersAndOrganizationsEditUser#DETAILS_EMAIL_ADDRESS_FIELD",
				value1 = ${searchTerm});
		}

		KeyPress(
			locator1 = "UsersAndOrganizationsEditUser#DETAILS_EMAIL_ADDRESS_FIELD",
			value1 = "\RETURN");

		if (${searchNoResults} == "true") {
			AssertTextEquals.assertPartialText(
				locator1 = "Message#EMPTY_INFO",
				value1 = "No users were found.");

			var key_userScreenName = ${userScreenName};

			AssertElementNotPresent(locator1 = "PortalDirectory#USER_TABLE_FIRST_NAME_LINK");

			AssertElementNotPresent(locator1 = "PortalDirectory#USER_TABLE_LAST_NAME_LINK");

			AssertElementNotPresent(locator1 = "PortalDirectory#USER_TABLE_SCREEN_NAME_LINK");
		}
		else {
			var key_userScreenName = ${userScreenName};

			AssertTextEquals(
				locator1 = "PortalDirectory#USER_TABLE_FIRST_NAME_LINK",
				value1 = ${userFirstName});

			AssertTextEquals(
				locator1 = "PortalDirectory#USER_TABLE_LAST_NAME_LINK",
				value1 = ${userLastName});

			AssertTextEquals(
				locator1 = "PortalDirectory#USER_TABLE_SCREEN_NAME_LINK",
				value1 = ${userScreenName});
		}
	}

	@summary = "Default summary"
	macro advancedSearchViaMySitesDirectoryPG(searchTerm = null, searchField = null, searchNoResults = null, userLastName = null, userScreenName = null, userFirstName = null) {
		Click(locator1 = "NavBar#SEARCH_TOGGLE_ADVANCED");

		if (${searchField} == "First Name") {
			Type(
				locator1 = "TextInput#FIRST_NAME",
				value1 = ${searchTerm});
		}
		else if (${searchField} == "Middle Name") {
			Type(
				locator1 = "TextInput#MIDDLE_NAME",
				value1 = ${searchTerm});
		}
		else if (${searchField} == "Last Name") {
			Type(
				locator1 = "TextInput#LAST_NAME",
				value1 = ${searchTerm});
		}
		else if (${searchField} == "Screen Name") {
			Type(
				locator1 = "TextInput#SCREEN_NAME",
				value1 = ${searchTerm});
		}
		else if (${searchField} == "Email Address") {
			Type(
				locator1 = "UsersAndOrganizationsEditUser#DETAILS_EMAIL_ADDRESS_FIELD",
				value1 = ${searchTerm});
		}

		KeyPress(
			locator1 = "UsersAndOrganizationsEditUser#DETAILS_EMAIL_ADDRESS_FIELD",
			value1 = "\RETURN");

		if (${searchNoResults} == "true") {
			AssertTextEquals.assertPartialText(
				locator1 = "Message#EMPTY_INFO",
				value1 = "No users were found.");

			var key_userScreenName = ${userScreenName};

			AssertElementNotPresent(locator1 = "PortalDirectory#USER_TABLE_FIRST_NAME_LINK");

			AssertElementNotPresent(locator1 = "PortalDirectory#USER_TABLE_LAST_NAME_LINK");

			AssertElementNotPresent(locator1 = "PortalDirectory#USER_TABLE_SCREEN_NAME_LINK");
		}
		else {
			var key_userScreenName = ${userScreenName};

			AssertTextEquals(
				locator1 = "PortalDirectory#USER_TABLE_FIRST_NAME_LINK",
				value1 = ${userFirstName});

			AssertTextEquals(
				locator1 = "PortalDirectory#USER_TABLE_LAST_NAME_LINK",
				value1 = ${userLastName});

			AssertTextEquals(
				locator1 = "PortalDirectory#USER_TABLE_SCREEN_NAME_LINK",
				value1 = ${userScreenName});
		}
	}

	@summary = "Default summary"
	macro advancedSearchViaPortalDirectoryPG(searchTerm = null, searchField = null, searchNoResults = null, userLastName = null, userScreenName = null, userFirstName = null) {
		Click(locator1 = "NavBar#SEARCH_TOGGLE_ADVANCED");

		if (${searchField} == "First Name") {
			Type(
				locator1 = "TextInput#FIRST_NAME",
				value1 = ${searchTerm});
		}
		else if (${searchField} == "Middle Name") {
			Type(
				locator1 = "TextInput#MIDDLE_NAME",
				value1 = ${searchTerm});
		}
		else if (${searchField} == "Last Name") {
			Type(
				locator1 = "TextInput#LAST_NAME",
				value1 = ${searchTerm});
		}
		else if (${searchField} == "Screen Name") {
			Type(
				locator1 = "TextInput#SCREEN_NAME",
				value1 = ${searchTerm});
		}
		else if (${searchField} == "Email Address") {
			Type(
				locator1 = "UsersAndOrganizationsEditUser#DETAILS_EMAIL_ADDRESS_FIELD",
				value1 = ${searchTerm});
		}

		KeyPress(
			locator1 = "UsersAndOrganizationsEditUser#DETAILS_EMAIL_ADDRESS_FIELD",
			value1 = "\RETURN");

		if (${searchNoResults} == "true") {
			AssertTextEquals.assertPartialText(
				locator1 = "Message#EMPTY_INFO",
				value1 = "No users were found.");

			var key_userScreenName = ${userScreenName};

			AssertElementNotPresent(locator1 = "PortalDirectory#USER_TABLE_FIRST_NAME_LINK");

			AssertElementNotPresent(locator1 = "PortalDirectory#USER_TABLE_LAST_NAME_LINK");

			AssertElementNotPresent(locator1 = "PortalDirectory#USER_TABLE_SCREEN_NAME_LINK");
		}
		else {
			var key_userScreenName = ${userScreenName};

			AssertTextEquals(
				locator1 = "PortalDirectory#USER_TABLE_FIRST_NAME_LINK",
				value1 = ${userFirstName});

			AssertTextEquals(
				locator1 = "PortalDirectory#USER_TABLE_LAST_NAME_LINK",
				value1 = ${userLastName});

			AssertTextEquals(
				locator1 = "PortalDirectory#USER_TABLE_SCREEN_NAME_LINK",
				value1 = ${userScreenName});
		}
	}

	@summary = "Default summary"
	macro advancedSearchViaSiteMembersDirectoryPG(searchTerm = null, searchField = null, searchNoResults = null, userLastName = null, userScreenName = null, userFirstName = null) {
		Click(locator1 = "NavBar#SEARCH_TOGGLE_ADVANCED");

		if (${searchField} == "First Name") {
			Type(
				locator1 = "TextInput#FIRST_NAME",
				value1 = ${searchTerm});
		}
		else if (${searchField} == "Middle Name") {
			Type(
				locator1 = "TextInput#MIDDLE_NAME",
				value1 = ${searchTerm});
		}
		else if (${searchField} == "Last Name") {
			Type(
				locator1 = "TextInput#LAST_NAME",
				value1 = ${searchTerm});
		}
		else if (${searchField} == "Screen Name") {
			Type(
				locator1 = "TextInput#SCREEN_NAME",
				value1 = ${searchTerm});
		}
		else if (${searchField} == "Email Address") {
			Type(
				locator1 = "UsersAndOrganizationsEditUser#DETAILS_EMAIL_ADDRESS_FIELD",
				value1 = ${searchTerm});
		}

		KeyPress(
			locator1 = "UsersAndOrganizationsEditUser#DETAILS_EMAIL_ADDRESS_FIELD",
			value1 = "\RETURN");

		if (${searchNoResults} == "true") {
			AssertTextEquals.assertPartialText(
				locator1 = "Message#EMPTY_INFO",
				value1 = "No users were found.");

			var key_userScreenName = ${userScreenName};

			AssertElementNotPresent(locator1 = "PortalDirectory#USER_TABLE_FIRST_NAME_LINK");

			AssertElementNotPresent(locator1 = "PortalDirectory#USER_TABLE_LAST_NAME_LINK");

			AssertElementNotPresent(locator1 = "PortalDirectory#USER_TABLE_SCREEN_NAME_LINK");
		}
		else {
			var key_userScreenName = ${userScreenName};

			AssertTextEquals(
				locator1 = "PortalDirectory#USER_TABLE_FIRST_NAME_LINK",
				value1 = ${userFirstName});

			AssertTextEquals(
				locator1 = "PortalDirectory#USER_TABLE_LAST_NAME_LINK",
				value1 = ${userLastName});

			AssertTextEquals(
				locator1 = "PortalDirectory#USER_TABLE_SCREEN_NAME_LINK",
				value1 = ${userScreenName});
		}
	}

	@summary = "Default summary"
	macro answerPasswordReminder() {
		if (IsElementPresent.pauseIsElementPresent(locator1 = "TextInput#REMINDER_QUERY")) {
			Type(
				locator1 = "TextInput#REMINDER_QUERY",
				value1 = "test");

			Click(locator1 = "Button#SUBMIT");
		}
	}

	@summary = "Default summary"
	macro answerPasswordReminderLocalized(localizedSaveButton = null, localizedAnswer = null) {
		var key_localizedAnswer = ${localizedAnswer};

		if (IsElementPresent.pauseIsElementPresent(locator1 = "TextInput#ANSWER_LOCALIZED")) {
			Type(
				locator1 = "TextInput#ANSWER_LOCALIZED",
				value1 = "test");

			Button.click(button = ${localizedSaveButton});

			Button.viewNotPresent(button = ${localizedSaveButton});
		}
	}

	@summary = "Default summary"
	macro assertScreenNameDisabled() {
		AssertClick(
			key_assetTitle = "Information",
			locator1 = "NavBar#ASSET_TITLE",
			value1 = "Information");

		AssertElementPresent(locator1 = "UsersAndOrganizationsEditUser#DETAILS_SCREEN_NAME_FIELD_DISABLED");
	}

	@summary = "Default summary"
	macro assertSection() {
		for (var sectionName : list ${sectionName}) {
			AssertClick(
				key_assetTitle = ${sectionName},
				locator1 = "NavBar#ASSET_TITLE",
				value1 = ${sectionName});
		}
	}

	@summary = "Default summary"
	macro assignAccountCP(userScreenName = null) {
		User.openUsersAdmin();

		UserNavigator.gotoUser(userScreenName = ${userScreenName});

		User.selectMembership(assetType = "Accounts");

		SelectFrame(
			key_title = "Select Account",
			locator1 = "IFrame#MODAL_ANY");

		for (var accountName : list ${accountName}) {
			Check(
				key_tableEntry = ${accountName},
				locator1 = "LexiconTable#TABLE_ENTRY_CHECKBOX");
		}

		SelectFrame(value1 = "relative=top");

		Button.clickAdd();

		PortletEntry.save();
	}

	@summary = "Default summary"
	macro assignAssetLibraryRole(roleTitle = null, userScreenName = null) {
		UserNavigator.gotoUser(userScreenName = ${userScreenName});

		UserNavigator.gotoRoles();

		AssertTextEquals(
			locator1 = "Message#EMPTY_DEPOT_ROLE",
			value1 = "This user is not assigned any asset library roles.");

		AssertClick(
			key_roleType = "DepotRole",
			locator1 = "UsersAndOrganizationsEditUser#ROLES_SELECT_LINK",
			value1 = "SELECT");

		SelectFrame(locator1 = "IFrame#SELECT_DEPOT_ROLE");

		AssertClick(
			locator1 = "UsersAndOrganizationsEditUser#DEPOT_ROLES_SELECT_BUTTON",
			roleTitle = ${roleTitle},
			value1 = "Choose");

		SelectFrame(value1 = "relative=top");

		AssertTextEquals(
			key_roleTitle = ${roleTitle},
			locator1 = "UsersAndOrganizationsEditUser#DEPOT_ROLES_TITLE",
			value1 = ${roleTitle});

		PortletEntry.save();
	}

	@summary = "Default summary"
	macro assignAssetLibraryRoleCMS(roleTitle = null, userScreenName = null) {
		UserNavigator.gotoUser(userScreenName = ${userScreenName});

		UserNavigator.gotoRoles();

		AssertTextEquals(
			locator1 = "Message#EMPTY_DEPOT_ROLE",
			value1 = "This user is not assigned any asset library or space roles.");

		AssertClick(
			key_roleType = "DepotRole",
			locator1 = "UsersAndOrganizationsEditUser#ROLES_SELECT_LINK",
			value1 = "SELECT");

		SelectFrame(locator1 = "IFrame#SELECT_DEPOT_ROLE");

		AssertClick(
			locator1 = "UsersAndOrganizationsEditUser#DEPOT_ROLES_SELECT_BUTTON",
			roleTitle = ${roleTitle},
			value1 = "Choose");

		SelectFrame(value1 = "relative=top");

		AssertTextEquals(
			key_roleTitle = ${roleTitle},
			locator1 = "UsersAndOrganizationsEditUser#DEPOT_ROLES_TITLE",
			value1 = ${roleTitle});

		PortletEntry.save();
	}

	@summary = "Default summary"
	macro assignOrgRoleCP(roleTitle = null, userScreenName = null) {
		UserNavigator.gotoUser(userScreenName = ${userScreenName});

		UserNavigator.gotoRoles();

		AssertClick(
			key_roleType = StringUtil.replace("Organization Role", " ", ""),
			locator1 = "UsersAndOrganizationsEditUser#ROLES_SELECT_LINK",
			value1 = "Select");

		SelectFrame(locator1 = "IFrame#SELECT_ORGANIZATION_ROLE");

		var key_roleTitle = ${roleTitle};

		AssertTextEquals(
			locator1 = "UsersAndOrganizationsEditUser#ROLES_SELECT_ROLE_NAME",
			value1 = ${roleTitle});

		AssertClick(
			locator1 = "UsersAndOrganizationsEditUser#ROLES_ORGANIZATION_SELECT_CHOOSE_BUTTON",
			value1 = "Choose");

		SelectFrame(value1 = "relative=top");

		AssertTextEquals(
			locator1 = "UsersAndOrganizationsEditUser#ROLES_SELECT_ROLE_NAME",
			value1 = ${roleTitle});

		PortletEntry.save();
	}

	@summary = "Default summary"
	macro assignRegularRoleCP(roleTitle = null, userScreenName = null) {
		UserNavigator.gotoUser(userScreenName = ${userScreenName});

		UserNavigator.gotoRoles();

		WaitForLiferayEvent.initializeLiferayEventLog();

		AssertClick(
			key_roleType = StringUtil.replace("Regular Role", " ", ""),
			locator1 = "UsersAndOrganizationsEditUser#ROLES_SELECT_LINK",
			value1 = "Select");

		SelectFrame(locator1 = "IFrame#SELECT_REGULAR_ROLE");

		var key_roleTitle = ${roleTitle};

		AssertTextEquals(
			locator1 = "UsersAndOrganizationsEditUser#ROLES_SELECT_ROLE_NAME",
			value1 = ${roleTitle});

		AssertClick(
			locator1 = "UsersAndOrganizationsEditUser#ROLES_REGULAR_SELECT_CHOOSE_BUTTON",
			value1 = "Choose");

		SelectFrame(value1 = "relative=top");

		AssertTextEquals(
			locator1 = "UsersAndOrganizationsEditUser#ROLES_SELECT_ROLE_NAME",
			value1 = ${roleTitle});

		PortletEntry.save();
	}

	@summary = "Default summary"
	macro assignSiteRoleCP(userScreenName = null, siteRoleName = null) {
		UserNavigator.gotoUser(userScreenName = ${userScreenName});

		UserNavigator.gotoRoles();

		AssertClick(
			key_roleType = StringUtil.replace("Site Role", " ", ""),
			locator1 = "UsersAndOrganizationsEditUser#ROLES_SELECT_LINK",
			value1 = "Select");

		SelectFrame(locator1 = "IFrame#SELECT_SITE_ROLE");

		var key_roleTitle = ${siteRoleName};
		var key_siteRoleName = ${siteRoleName};

		AssertTextEquals(
			locator1 = "UsersAndOrganizationsEditUser#ROLES_SITE_ROLES_TABLE_TITLE",
			value1 = ${siteRoleName});

		AssertClick(
			locator1 = "UsersAndOrganizationsEditUser#ROLES_REGULAR_SELECT_CHOOSE_BUTTON",
			value1 = "Choose");

		SelectFrame(value1 = "relative=top");

		AssertTextEquals(
			locator1 = "UsersAndOrganizationsEditUser#ROLES_SITE_ROLES_TABLE_TITLE",
			value1 = ${siteRoleName});

		PortletEntry.save();
	}

	@summary = "Default summary"
	macro assignUserGroupCP(userGroupName = null) {
		User.selectMembership(assetType = "User Groups");

		SelectFrame(locator1 = "IFrame#SELECT_USER_GROUP");

		AssertTextEquals(
			key_rowEntry = ${userGroupName},
			locator1 = "ContentRow#ENTRY_CONTENT_ENTRY_NAME",
			value1 = ${userGroupName});

		Click(
			key_card = ${userGroupName},
			locator1 = "Card#CARD");

		SelectFrame(value1 = "relative=top");

		AssertTextEquals(
			key_assetType = "userGroups",
			key_assigneeName = ${userGroupName},
			locator1 = "UsersAndOrganizationsEditUser#MEMBERSHIPS_TABLE_ENTRY_NAME",
			value1 = ${userGroupName});

		AssertElementPresent(
			key_assetType = "userGroups",
			key_assigneeName = ${userGroupName},
			locator1 = "UsersAndOrganizationsEditUser#MEMBERSHIPS_TABLE_ENTRY_REMOVE_ASSIGNEE_ICON");

		PortletEntry.save();
	}

	@summary = "Default summary"
	macro attemptImpersonateCP(userScreenName = null) {
		if (isSet(notAdmin)) {
			AssertElementNotPresent(
				key_tableEntry = ${userScreenName},
				locator1 = "LexiconTable#TABLE_ENTRY_ELLIPSIS");

			AssertElementNotPresent(locator1 = "UsersAndOrganizations#USER_TABLE_IMPERSONATE");
		}
		else {
			Click(
				key_tableEntry = ${userScreenName},
				locator1 = "LexiconTable#TABLE_ENTRY_ELLIPSIS");

			MenuItem.viewNotPresent(menuItem = "Impersonate User");
		}
	}

	@summary = "Default summary"
	macro basicSearchCP(userLastName = null, userScreenName = null, userBasicSearch = null, userFirstName = null, userName = null) {
		if (${userBasicSearch} == "Basic") {
			Search.searchCP(searchTerm = ${userScreenName});

			var key_userScreenName = ${userScreenName};

			AssertTextEquals(
				locator1 = "UsersAndOrganizations#USER_TABLE_NAME",
				value1 = ${userName});

			AssertTextEquals(
				locator1 = "UsersAndOrganizations#USER_TABLE_SCREEN_NAME",
				value1 = ${userScreenName});

			Search.searchCP(searchTerm = "user1*");

			AssertTextEquals.assertPartialText(
				locator1 = "Message#EMPTY_INFO",
				value1 = "No users were found.");
		}
		else if (${userBasicSearch} == "Full Name") {
			var fullName = '''"${userFirstName} ${userLastName}"''';

			Search.searchCP(searchTerm = ${fullName});

			var key_userScreenName = ${userScreenName};

			AssertTextEquals(
				locator1 = "UsersAndOrganizations#USER_TABLE_NAME",
				value1 = ${userName});

			AssertTextEquals(
				locator1 = "UsersAndOrganizations#USER_TABLE_SCREEN_NAME",
				value1 = ${userScreenName});

			var fullName = '''"${userFirstName} 1 ${userLastName}"''';

			Search.searchCP(searchTerm = ${fullName});

			AssertTextEquals.assertPartialText(
				locator1 = "Message#EMPTY_INFO",
				value1 = "No users were found.");
		}
		else if (${userBasicSearch} == "Quotes") {
			var quotes = '''"${userScreenName}"''';

			Search.searchCP(searchTerm = ${quotes});

			var key_userScreenName = ${userScreenName};

			AssertTextEquals(
				locator1 = "UsersAndOrganizations#USER_TABLE_NAME",
				value1 = ${userName});

			AssertTextEquals(
				locator1 = "UsersAndOrganizations#USER_TABLE_SCREEN_NAME",
				value1 = ${userScreenName});
		}
	}

	@summary = "Default summary"
	macro bulkActivateCP(userFirstNameList = null) {
		if (isSet(userFirstNameList)) {
			for (var userFirstName : list ${userFirstNameList}) {
				LexiconEntry.selectEntry(rowEntry = ${userFirstName});
			}
		}
		else {
			PortletEntry.selectAll();

			if (IsChecked(key_rowEntry = "Test Test", locator1 = "ContentRow#ENTRY_CONTENT_ENTRY_CHECKBOX")) {
				Click(
					key_rowEntry = "Test Test",
					locator1 = "ContentRow#ENTRY_CONTENT_ENTRY_CHECKBOX");
			}

			AssertNotChecked.assertNotCheckedNotVisible(
				key_rowEntry = "Test Test",
				locator1 = "ContentRow#ENTRY_CONTENT_ENTRY_CHECKBOX");
		}

		Click(locator1 = "ManagementBar#ELLIPSIS");

		MenuItem.clickNoError(menuItem = "Activate");

		Alert.viewSuccessMessage();
	}

	@summary = "Default summary"
	macro bulkDeactivateCP(deactivateType = null, userFirstNameList = null) {
		if (isSet(userFirstNameList)) {
			for (var userFirstName : list ${userFirstNameList}) {
				LexiconEntry.selectEntry(rowEntry = ${userFirstName});
			}
		}
		else {
			PortletEntry.selectAll();

			if (IsChecked(key_rowEntry = "Test Test", locator1 = "ContentRow#ENTRY_CONTENT_ENTRY_CHECKBOX")) {
				Click(
					key_rowEntry = "Test Test",
					locator1 = "ContentRow#ENTRY_CONTENT_ENTRY_CHECKBOX");
			}

			AssertNotChecked.assertNotCheckedNotVisible(
				key_rowEntry = "Test Test",
				locator1 = "ContentRow#ENTRY_CONTENT_ENTRY_CHECKBOX");
		}

		if (${deactivateType} == "Management Toolbar") {
			Click(locator1 = "ManagementBar#ELLIPSIS");

			MenuItem.clickNoError(menuItem = "Deactivate");
		}
		else {
			ClickNoError(
				key_text = "hidden",
				locator1 = "ManagementBar#ANY_ICON");
		}

		AssertConfirm(value1 = "Are you sure you want to deactivate the selected users?");

		Alert.viewSuccessMessage();
	}

	@summary = "Default summary"
	macro bulkDeleteCP(deactivateType = null, userFirstNameList = null) {
		if (isSet(userFirstNameList)) {
			for (var userFirstName : list ${userFirstNameList}) {
				LexiconEntry.selectEntry(rowEntry = ${userFirstName});
			}
		}
		else {
			PortletEntry.selectAll();
		}

		if (${deactivateType} == "Management Toolbar") {
			Click(locator1 = "ManagementBar#ELLIPSIS");

			MenuItem.clickNoError(menuItem = "Delete");
		}
		else {
			ClickNoError(
				key_text = "times-circle",
				locator1 = "ManagementBar#ANY_ICON");
		}

		AssertConfirm.waitForConfirmation(value1 = "Are you sure you want to permanently delete the selected users?");

		Alert.viewSuccessMessage();
	}

	@summary = "Default summary"
	macro changePasswordCP(userPassword = null, authenticationMethod = null, userScreenName = null, userEmailAddress = null, newPassword = null, nodePort = null) {
		UserNavigator.gotoPassword();

		if (${userPassword} == "Invalid") {
			Type(
				locator1 = "TextInput#CURRENT_PASSWORD",
				value1 = "");

			Type(
				locator1 = "TextInput#NEW_PASSWORD",
				value1 = "test1");

			Type(
				locator1 = "TextInput#REENTER_PASSWORD",
				value1 = "test1");

			Button.clickSave();

			Alert.viewSpecificRequiredField(fieldFieldLabel = "Current Password");

			User.logoutPG();

			User.loginUserPG(
				password = PropsUtil.get("default.admin.password"),
				userEmailAddress = ${userEmailAddress});

			User.logoutPG();

			User.loginPG(
				authenticationMethod = ${authenticationMethod},
				newPassword = ${newPassword},
				nodePort = ${nodePort},
				password = PropsUtil.get("default.admin.password"),
				userEmailAddress = "test@liferay.com",
				userScreenName = ${userScreenName});
		}
		else {
			Type(
				locator1 = "TextInput#CURRENT_PASSWORD",
				value1 = "test");

			Type(
				locator1 = "TextInput#NEW_PASSWORD",
				value1 = "test1");

			Type(
				locator1 = "TextInput#REENTER_PASSWORD",
				value1 = "test1");

			PortletEntry.save();

			User.logoutPG();

			User.loginUserPG(
				password = "test1",
				userEmailAddress = ${userEmailAddress});

			User.logoutPG();

			User.loginPG(
				authenticationMethod = ${authenticationMethod},
				newPassword = ${newPassword},
				nodePort = ${nodePort},
				password = PropsUtil.get("default.admin.password"),
				userEmailAddress = "test@liferay.com",
				userScreenName = ${userScreenName});
		}
	}

	@summary = "Default summary"
	macro changeProfileImage(imageName = null) {
		Click(
			ariaLabel = "Change Image",
			locator1 = "Button#ANY_WITH_ARIA_LABEL");

		IFrame.selectModalFrame();

		AssertElementPresent(locator1 = "UsersAndOrganizationsEditUser#UPLOAD_IMAGE_SELECT");

		UploadCommonFile.uploadCommonFileHiddenNoMouseOver(
			locator1 = "TextInput#UPLOAD_FILE",
			value1 = ${imageName});

		WaitForElementPresent(locator1 = "UsersAndOrganizationsEditUser#USER_PORTRAIT_CROPPER");

		WaitForElementNotPresent(
			key_text = "Done",
			locator1 = "Button#ANY_DISABLED");

		Button.click(button = "Done");

		IFrame.selectTopFrame();

		AssertVisible(
			key_imageName = ${imageName},
			locator1 = "UsersAndOrganizationsEditUser#DETAILS_PROFILE_IMAGE");
	}

	@summary = "Default summary"
	macro configureCustomPasswordField(baseURL = null, enableCustomPassword = null) {
		PortalSettings.gotoConfiguration(
			baseURL = ${baseURL},
			configurationCategory = "Users",
			configurationName = "Fields",
			configurationScope = "Virtual Instance Scope");

		if (${enableCustomPassword} == "true") {
			FormFields.enableCheckbox(fieldName = "login.create.account.allow.custom.password");
		}
		else {
			FormFields.disableCheckbox(fieldName = "login.create.account.allow.custom.password");
		}

		if (IsElementPresent(locator1 = "Button#UPDATE")) {
			PortletEntry.update();
		}
		else if (IsElementPresent(locator1 = "Button#SAVE")) {
			PortletEntry.save();
		}
	}

	@summary = "Default summary"
	macro confirmPassword(password = null) {
		SelectFrame.selectFrameNoLoading(locator1 = "IFrame#CONFIRM_PASSWORD");

		Click(locator1 = "TextInput#PASSWORD_CONFIRM");

		Type(
			locator1 = "TextInput#PASSWORD_CONFIRM",
			value1 = ${password});

		Button.clickConfirm();

		SelectFrameTop();

		Alert.viewSuccessMessage();

		WaitForLiferayEvent.initializeLiferayEventLog();
	}

	@summary = "Default summary"
	macro createAccount(userLastName = null, userScreenName = null, userEmailAddress = null, newPassword = null, userFirstName = null) {
		SignInNavigator.gotoCreateAccount();

		Type(
			locator1 = "TextInput#FIRST_NAME",
			value1 = ${userFirstName});

		Type(
			locator1 = "TextInput#LAST_NAME",
			value1 = ${userLastName});

		Type(
			locator1 = "TextInput#SCREEN_NAME",
			value1 = ${userScreenName});

		Type(
			locator1 = "TextInput#EMAIL_ADDRESS",
			value1 = ${userEmailAddress});

		Type(
			locator1 = "TextInput#PASSWORD",
			value1 = ${newPassword});

		Type(
			locator1 = "TextInput#REENTER_PASSWORD",
			value1 = ${newPassword});

		Button.clickSave();

		Alert.viewSuccessMessageText(successMessage = "Thank you for creating an account. Use your password to log in.");
	}

	@summary = "Default summary"
	macro createMultipleUsers(letter = null) {
		for (var letter : list ${letter}) {
			User.createUser(
				userEmailAddress = "${letter}@liferay.com",
				userFirstName = ${letter},
				userLastName = ${letter},
				userScreenName = ${letter});

			Navigator.gotoBack();
		}
	}

	@summary = "Default summary"
	macro createUser(lng = null, userEmailAddress = null, userFirstName = null, userJobTitle = null, userComment = null, userTag = null, userLastName = null, userScreenName = null, userMiddleName = null, userGender = null, lat = null, userBirthday = null,) {
		LexiconEntry.gotoAdd();

		User._editUserInformation(
			userBirthday = ${userBirthday},
			userComment = ${userComment},
			userEmailAddress = ${userEmailAddress},
			userFirstName = ${userFirstName},
			userGender = ${userGender},
			userJobTitle = ${userJobTitle},
			userLastName = ${userLastName},
			userMiddleName = ${userMiddleName},
			userScreenName = ${userScreenName},
			userTag = ${userTag});

		if (isSet(lat) && isSet(lng)) {
			Geolocation.setMarkerPosition(
				lat = ${lat},
				lng = ${lng});
		}

		var browserType = PropsUtil.get("browser.type");

		if (${browserType} == "safari") {
			Button.clickSave();
		}
		else {
			AssertClick(
				locator1 = "Button#SUBMIT",
				value1 = "Save");
		}
	}

	@summary = "Default summary"
	macro deactivateCP(deactivateType = null, userFirstName = null, userFirstNameList = null) {
		if (${deactivateType} == "Actions") {
			User.singleDeactivateCP(userFirstName = ${userFirstName});
		}
		else {
			User.bulkDeactivateCP(
				deactivateType = ${deactivateType},
				userFirstNameList = ${userFirstNameList});
		}
	}

	@summary = "Default summary"
	macro deactivateMultipleUsers(letter = null) {
		for (var letter : list ${letter}) {
			User.deactivateCP(
				deactivateType = "Actions",
				userFirstName = ${letter});
		}
	}

	@summary = "Default summary"
	macro deleteCP(deactivateType = null, userFirstName = null, userFirstNameList = null) {
		ManagementBar.setFilterAndOrder(filterBy = "Inactive");

		if (${deactivateType} == "Actions") {
			User.singleDeleteCP(userFirstName = ${userFirstName});
		}
		else {
			User.bulkDeleteCP(
				deactivateType = ${deactivateType},
				userFirstNameList = ${userFirstNameList});
		}
	}

	@summary = "Default summary"
	macro deletePersonalDataCP(userLastName = null, userFirstName = null) {
		LexiconEntry.gotoEntryMenuItemNoError(
			menuItem = "Delete Personal Data",
			rowEntry = ${userFirstName});

		AssertConfirm(value1 = "The user must be deactivated before starting the data erasure process. Are you sure you want to deactivate the user?");

		Alert.viewSuccessMessage();

		AssertElementPresent(
			key_firstName = ${userFirstName},
			key_lastName = ${userLastName},
			locator1 = "DataErasure#HEADER");
	}

	@summary = "Default summary"
	macro deleteProfileImage() {
		if (!(IsElementPresent(key_title = "Clear Image", locator1 = "Button#ANY_TITLE_DISABLED"))) {
			Click(
				ariaLabel = "Clear Image",
				locator1 = "Button#ANY_WITH_ARIA_LABEL");
		}

		AssertVisible(
			key_imageName = "user_portrait",
			locator1 = "UsersAndOrganizationsEditUser#DETAILS_PROFILE_IMAGE");
	}

	@summary = "Default summary"
	macro editAddressCP(country = null, city = null, addressType = null, postalCode = null, street1 = null) {
		UserNavigator.gotoContact();

		LexiconEntry.gotoVerticalEllipsisMenuItemNoError(menuItem = "Edit");

		if (isSet(addressType)) {
			Select(
				locator1 = "Select#TYPE",
				value1 = ${addressType});
		}

		if (isSet(street1)) {
			Type(
				locator1 = "TextInput#STREET_1",
				value1 = ${street1});
		}

		if (isSet(city)) {
			Type(
				locator1 = "TextInput#CITY",
				value1 = ${city});
		}

		if (isSet(country)) {
			Select(
				locator1 = "Select#COUNTRY",
				value1 = ${country});
		}

		if (isSet(postalCode)) {
			Type(
				locator1 = "TextInput#POSTAL_CODE",
				value1 = ${postalCode});
		}

		PortletEntry.save();
	}

	@summary = "Default summary"
	macro editCustomFieldInformation(userFirstNameEdit = null, userScreenName = null, customFieldValue = null, customFieldName = null, customFieldType = null) {
		UserNavigator.gotoUser(userScreenName = ${userScreenName});

		if ((${customFieldType} == "Checkbox") || (${customFieldType} == "Radio")) {
			Click(
				key_customFieldName = ${customFieldName},
				key_customFieldValue = ${customFieldValue},
				locator1 = "UsersAndOrganizationsEditUser#DETAILS_CUSTOM_FIELD_INPUT",
				value1 = ${userFirstNameEdit});
		}

		if (${customFieldType} == "Dropdown") {
			Select(
				key_customFieldName = ${customFieldName},
				locator1 = "UsersAndOrganizationsEditUser#DETAILS_CUSTOM_FIELD_SELECT",
				value1 = ${customFieldValue});
		}

		PortletEntry.save();
	}

	@summary = "Default summary"
	macro editDetailsViaProfile(country = null, city = null, addressType = null, postalCode = null, street1 = null) {
		Click.javaScriptClick(locator1 = "Icon#EDIT_PENCIL");

		Portlet.viewSpecificPG(portletName = "Account Settings");

		AssertElementPresent.assertElementPresent(locator1 = "TextInput#FIRST_NAME");

		if (isSet(userContact)) {
			User.editAddressCP(
				addressType = ${addressType},
				city = ${city},
				country = ${country},
				postalCode = ${postalCode},
				street1 = ${street1});
		}
	}

	@summary = "Default summary"
	macro editDisplaySettingsCP(userScreenName = null, languageName = null) {
		LexiconTable.clickEllipsisItem(
			item = "Edit",
			tableEntry = ${userScreenName});

		Select(
			locator1 = "Select#DEFAULT_LANGUAGE",
			value1 = ${languageName});

		PortletEntry.save();
	}

	@summary = "Default summary"
	macro editEmailAddressCP(password = null, userScreenName = null, userEmailAddress = null) {
		UserNavigator.gotoUser(userScreenName = ${userScreenName});

		Type(
			locator1 = "UsersAndOrganizationsEditUser#DETAILS_EMAIL_ADDRESS_FIELD",
			value1 = ${userEmailAddress});

		Button.clickSave();

		SelectFrame.selectFrameNoLoading(locator1 = "IFrame#CONFIRM_PASSWORD");

		Type(
			locator1 = "UsersAndOrganizationsEditUser#PASSWORD_CONFIRM_PASSWORD_FIELD",
			value1 = ${password});

		Button.clickConfirm();

		Alert.viewSuccessMessage();
	}

	@summary = "Default summary"
	macro editPassword(requirePasswordReset = null, currentPassword = null) {
		UserNavigator.gotoPassword();

		if (isSet(currentPassword)) {
			Type(
				locator1 = "TextInput#CURRENT_PASSWORD",
				value1 = ${currentPassword});
		}

		if (!(isSet(newPassword))) {
			var newPassword = "password";
		}

		Type(
			locator1 = "TextInput#NEW_PASSWORD",
			value1 = ${newPassword});

		Type(
			locator1 = "TextInput#REENTER_PASSWORD",
			value1 = ${newPassword});

		if (IsElementPresent(checkboxName = "Require Password Reset", locator1 = "Checkbox#ANY_CHECKBOX")) {
			if ((${requirePasswordReset} == "true") && (IsNotChecked(checkboxName = "Require Password Reset", locator1 = "Checkbox#ANY_CHECKBOX"))) {
				Check(
					checkboxName = "Require Password Reset",
					locator1 = "Checkbox#ANY_CHECKBOX");
			}
			else if (!(isSet(requirePasswordReset)) || (${requirePasswordReset} == "false")) {
				Uncheck(
					checkboxName = "Require Password Reset",
					locator1 = "Checkbox#ANY_CHECKBOX");
			}
		}

		PortletEntry.save();
	}

	@summary = "Default summary"
	macro editPasswordCP(authenticationMethod = null, userScreenName = null, requirePasswordReset = null, newPassword = null, userEmailAddress = null, nodePort = null, samlEnabled = null, currentPassword = null) {
		JSONUserAPI._disablePasswordChangeRequired();

		UserNavigator.gotoUser(userScreenName = ${userScreenName});

		User.editPassword(
			currentPassword = ${currentPassword},
			newPassword = ${newPassword},
			requirePasswordReset = ${requirePasswordReset});

		User.logoutPG();

		if (${samlEnabled} == "true") {
			User.viewLoggedOutPG();
		}

		User.loginPG(
			authenticationMethod = ${authenticationMethod},
			newPassword = ${newPassword},
			nodePort = ${nodePort},
			password = ${newPassword},
			userEmailAddress = ${userEmailAddress},
			userScreenName = ${userScreenName});

		User.logoutPG();

		if (${samlEnabled} == "true") {
			User.viewLoggedOutPG();
		}

		User.loginPG(
			authenticationMethod = ${authenticationMethod},
			nodePort = ${nodePort},
			password = PropsUtil.get("default.admin.password"),
			userEmailAddress = "test@liferay.com",
			userScreenName = ${userScreenName});
	}

	@summary = "Default summary"
	macro editPasswordInvalidCP(passwordTooShort = null, regex = null, userScreenName = null, minimumNumbers = null) {
		UserNavigator.gotoUser(userScreenName = ${userScreenName});

		if (!(isSet(newPassword))) {
			var newPassword = "password";
		}

		UserNavigator.gotoPassword();

		Type(
			locator1 = "UsersAndOrganizationsEditUser#PASSWORD_NEW_PASSWORD_FIELD",
			value1 = ${newPassword});

		Type(
			locator1 = "UsersAndOrganizationsEditUser#PASSWORD_ENTER_AGAIN_FIELD",
			value1 = ${newPassword});

		Button.clickSave();

		if (${passwordTooShort} == "true") {
			AssertTextEquals.assertPartialText(
				locator1 = "Message#ERROR_1",
				value1 = "That password is too short. Please make sure your password is at least 6 characters long.");
		}
		else if (isSet(minimumNumbers)) {
			AssertTextEquals.assertPartialText(
				locator1 = "Message#ERROR_1",
				value1 = "That password must contain at least ${minimumNumbers} numbers.");
		}
		else if (isSet(regex)) {
			AssertTextEquals.assertPartialText(
				locator1 = "Message#ERROR_1",
				value1 = "That password does not comply with the regular expression ${regex}. Please enter a different password.");
		}
		else {
			AssertTextEquals.assertPartialText(
				locator1 = "Message#ERROR_1",
				value1 = "That password is invalid. Please enter in a different password.");
		}
	}

	@summary = "Default summary"
	macro editPrimary(makePrimary = null) {
		if (${makePrimary} == "true") {
			Check(
				checkboxName = "Make Primary",
				locator1 = "Checkbox#ANY_CHECKBOX");
		}
		else if (${makePrimary} == "false") {
			Uncheck(
				checkboxName = "Make Primary",
				locator1 = "Checkbox#ANY_CHECKBOX");
		}

		Click(locator1 = "Button#SAVE");

		Alert.viewSuccessMessage();
	}

	@summary = "Default summary"
	macro editUserAlertsAndAnnouncementsDeliveryCP(checkbox = null, deliveryOptions = null) {
		UserNavigator.gotoAlertsAndAnnouncementsDelivery();

		if (${checkbox} == "Enable") {
			for (var deliveryOption : list ${deliveryOptions}) {
				Check(
					key_tableEntry = ${deliveryOption},
					locator1 = "LexiconTable#TABLE_ENTRY_CHECKBOX");
			}
		}

		PortletEntry.save();
	}

	@summary = "Default summary"
	macro editUserAssetLibraryCP(depotName = null, userScreenName = null) {
		UserNavigator.gotoUser(userScreenName = ${userScreenName});

		User.selectMembership(assetType = "Asset Libraries");

		SelectFrame(locator1 = "IFrame#MODAL_BODY");

		LexiconEntry.gotoEntry(rowEntry = ${depotName});

		SelectFrame(value1 = "relative=top");

		AssertTextEquals(
			key_assetType = "depotGroups",
			key_assigneeName = ${depotName},
			locator1 = "UsersAndOrganizationsEditUser#MEMBERSHIPS_TABLE_ENTRY_NAME",
			value1 = ${depotName});

		PortletEntry.save();
	}

	@summary = "Default summary"
	macro editUserDisplaySettingsCP(displaySettingsGreeting = null, userScreenName = null) {
		var key_userScreenName = ${userScreenName};

		AssertTextEquals(
			locator1 = "UsersAndOrganizations#USER_TABLE_SCREEN_NAME",
			value1 = ${userScreenName});

		AssertClick(
			locator1 = "UsersAndOrganizations#USER_TABLE_SCREEN_NAME",
			value1 = ${userScreenName});

		User.editUserDisplaySettingsTimeZonePacific(displaySettingsGreeting = ${displaySettingsGreeting});
	}

	@summary = "Default summary"
	macro editUserDisplaySettingsTimeZonePacific(displaySettingsGreeting = null) {
		UserNavigator.gotoDisplaySettings();

		if (IsTextNotEqual(locator1 = "Select#LOS_ANGELES_TIME_ZONE", value1 = "(UTC -07:00) Pacific Daylight Time")) {
			Select(
				locator1 = "Select#TIME_ZONE",
				value1 = "(UTC -08:00) Pacific Standard Time");
		}
		else {
			Select(
				locator1 = "Select#TIME_ZONE",
				value1 = "(UTC -07:00) Pacific Daylight Time");
		}

		Type(
			locator1 = "UsersAndOrganizationsEditUser#DISPLAY_SETTINGS_GREETING_FIELD",
			value1 = ${displaySettingsGreeting});

		PortletEntry.save();

		UserNavigator.gotoDisplaySettings();

		if (IsTextNotEqual(locator1 = "Select#LOS_ANGELES_TIME_ZONE", value1 = "(UTC -07:00) Pacific Daylight Time")) {
			AssertSelectedLabel(
				locator1 = "Select#TIME_ZONE",
				value1 = "(UTC -08:00) Pacific Standard Time");
		}
		else {
			AssertSelectedLabel(
				locator1 = "Select#TIME_ZONE",
				value1 = "(UTC -07:00) Pacific Daylight Time");
		}

		AssertTextEquals(
			locator1 = "UsersAndOrganizationsEditUser#DISPLAY_SETTINGS_GREETING_FIELD",
			value1 = ${displaySettingsGreeting});
	}

	@summary = "Default summary"
	macro editUserGenderCP(userGender = null, userName = null) {
		LexiconEntry.gotoEntry(rowEntry = ${userName});

		Select(
			locator1 = "UsersAndOrganizationsEditUser#DETAILS_GENDER_SELECT",
			value1 = ${userGender});

		PortletEntry.save();
	}

	@summary = "Default summary"
	macro editUserInformation(password = null, userFirstNameEdit = null, userLastNameEdit = null, userCustomFieldLabel = null, userCustomField = null, userCustomFieldType = null, userEmailAddressEdit = null, userMiddleNameEdit = null, userScreenNameEdit = null) {
		AssertElementPresent.assertElementPresent(locator1 = "TextInput#FIRST_NAME");

		if (isSet(userFirstNameEdit)) {
			Type(
				locator1 = "TextInput#FIRST_NAME",
				value1 = ${userFirstNameEdit});
		}

		if (isSet(userLastNameEdit)) {
			Type(
				locator1 = "TextInput#LAST_NAME",
				value1 = ${userLastNameEdit});
		}

		if (isSet(userMiddleNameEdit)) {
			Type(
				locator1 = "TextInput#MIDDLE_NAME",
				value1 = ${userMiddleNameEdit});
		}

		if (isSet(userCustomField)) {
			if (${userCustomFieldType} == "Text Area") {
				Type(
					locator1 = "TextInput#CUSTOM_FIELD",
					value1 = ${userCustomField});
			}
			else {
				Type(
					key_fieldLabel = ${userCustomFieldLabel},
					locator1 = "TextInput#GENERIC_TEXT_INPUT",
					value1 = ${userCustomField});
			}
		}

		PortletEntry.save();

		if (isSet(userScreenNameEdit)) {
			Type(
				locator1 = "TextInput#SCREEN_NAME",
				value1 = ${userScreenNameEdit});

			Button.clickSave();

			User.confirmPassword(password = ${password});
		}

		if (isSet(userEmailAddressEdit)) {
			Type(
				locator1 = "TextInput#EMAIL_ADDRESS",
				value1 = ${userEmailAddressEdit});

			Button.clickSave();

			User.confirmPassword(password = ${password});
		}
	}

	@summary = "Default summary"
	macro editUserJobTitleCP(userJobTitle = null, userName = null) {
		LexiconEntry.gotoEntry(rowEntry = ${userName});

		Type(
			locator1 = "UsersAndOrganizationsEditUser#DETAILS_JOB_TITLE_FIELD",
			value1 = ${userJobTitle});

		PortletEntry.save();
	}

	@summary = "Default summary"
	macro editUserSiteCP(userScreenName = null, siteName = null) {
		UserNavigator.gotoUser(userScreenName = ${userScreenName});

		User.selectMembership(assetType = "Sites");

		SelectFrame(locator1 = "IFrame#SELECT_USER_SITES");

		var key_siteName = ${siteName};

		Type(
			locator1 = "TextInput#SITE_SEARCH_KEYWORDS",
			value1 = ${siteName});

		var browserType = PropsUtil.get("browser.type");

		if (${browserType} != "safari") {
			KeyPress(
				locator1 = "TextInput#SEARCH",
				value1 = "\RETURN");

			AssertTextEquals(
				key_rowEntry = ${siteName},
				locator1 = "ContentRow#ENTRY_CONTENT_ENTRY_NAME",
				value1 = ${siteName});

			Click(locator1 = "SitesSelectSite#SITES_TABLE_CHOOSE_SITE_LINK");
		}
		else {
			Click.javaScriptClick(locator1 = "SitesSelectSite#SITES_TABLE_CHOOSE_SITE_LINK");
		}

		SelectFrame(value1 = "relative=top");

		AssertTextEquals(
			key_assetType = "groups",
			key_assigneeName = ${siteName},
			locator1 = "UsersAndOrganizationsEditUser#MEMBERSHIPS_TABLE_ENTRY_NAME",
			value1 = ${siteName});

		PortletEntry.save();
	}

	@summary = "Default summary"
	macro editUserSuffixCP(userSuffix = null, userName = null) {
		LexiconEntry.gotoEntry(rowEntry = ${userName});

		Select(
			locator1 = "UsersAndOrganizationsEditUser#DETAILS_SUFFIX_SELECT",
			value1 = ${userSuffix});

		PortletEntry.save();
	}

	@summary = "Default summary"
	macro editUserTagCP(addTagMethod = null, tagNameList = null, userName = null) {
		LexiconEntry.gotoEntry(rowEntry = ${userName});

		if (${addTagMethod} == "Select") {
			User.selectUserTagCP(tagNameList = ${tagNameList});
		}
		else {
			for (var tagName : list ${tagNameList}) {
				AssetCategorization.addTag(tagName = ${tagName});
			}
		}

		PortletEntry.save();
	}

	@summary = "Default summary"
	macro exportPersonalDataCP(userLastName = null, userFirstName = null) {
		LexiconEntry.gotoEntryMenuItemNoError(
			menuItem = "Export Personal Data",
			rowEntry = ${userFirstName});

		AssertElementPresent(
			key_firstName = ${userFirstName},
			key_lastName = ${userLastName},
			locator1 = "ExportPersonalData#HEADER");
	}

	@summary = "Default summary"
	macro fillResetPasswordFields(password = null) {
		Type(
			locator1 = "TextInput#PASSWORD_1",
			value1 = ${password});

		Type(
			locator1 = "TextInput#PASSWORD_2",
			value1 = ${password});
	}

	@summary = "Default summary"
	macro firstLoginByScreenNamePG(virtualHostsURL = null, userScreenName = null, userEmailAddress = null, newPassword = null) {
		if (isSet(virtualHostsURL)) {
			Navigator.openSpecificURL(url = ${virtualHostsURL});
		}
		else {
			Navigator.openURL();
		}

		UserBar.signOut();

		JSONUser.agreeToTermsAndAnswerReminderQueryByScreenName(
			portalURL = ${virtualHostsURL},
			userScreenName = ${userScreenName});

		if (isSet(virtualHostsURL)) {
			Navigator.openSpecificURL(url = "${virtualHostsURL}/web/guest?SM_USER=${userEmailAddress}");
		}
		else {
			var baseURL = JSONCompany.getPortalURL();

			Navigator.openSpecificURL(url = "${baseURL}/web/guest?SM_USER=${userScreenName}");
		}

		// TODO Remove below macro after password reminder gets completely cleaned out with JSON

		User.resetPassword(newPassword = ${newPassword});

		User.answerPasswordReminder();

		AssertElementPresent(locator1 = "UserBar#USER_AVATAR_IMAGE");

		WaitForLiferayEvent.initializeLiferayEventLog();
	}

	@summary = "Default summary"
	macro firstLoginPG(idpName = null, domain = null, virtualHostsURL = null, userScreenName = null, newPassword = null, rememberMeChecked = null) {
		var appServerType = PropsUtil.get("app.server.type");
		var password = JSONUtil2.getDefaultUserFirstPassword(customDefaultUserFirstPassword = ${password});
		var userEmailAddress = JSONUtil2.getDefaultUserEmail(customDefaultUserEmail = ${userEmailAddress});

		if (${appServerType} == "weblogic") {

			// Pausing 2 seconds due to LPS-159595

			Pause(value1 = 2000);
		}

		if (isSet(virtualHostsURL)) {
			Navigator.openSpecificURL(
				domain = ${domain},
				url = ${virtualHostsURL});
		}
		else {
			Navigator.openURL();
		}

		UserBar.signOut();

		if (isSet(virtualHostsURL)) {
			var portalURL = ${virtualHostsURL};
		}
		else {
			var portalURL = JSONCompany.getPortalURL();
		}

		var isLXC = JSONUtil2.getLXCEnvironment();

		if (${isLXC} != "true") {

			// Workaround for LXC not supporting JSON call currently

			JSONUser.agreeToTermsAndAnswerReminderQuery(
				password = ${password},
				portalURL = ${portalURL},
				userEmailAddress = ${userEmailAddress});
		}

		if (isSet(virtualHostsURL)) {
			Navigator.openSpecificURL(url = "${virtualHostsURL}/web/guest?SM_USER=${userEmailAddress}");
		}
		else {
			var baseURL = JSONCompany.getPortalURL();

			Navigator.openSpecificURL(url = "${baseURL}/web/guest?SM_USER=${userEmailAddress}");
		}

		// TODO Remove below macro after password reminder gets completely cleaned out with JSON

		User.resetPassword(newPassword = ${newPassword});

		User.answerPasswordReminder();

		if (IsElementNotPresent(locator1 = "UserBar#USER_AVATAR_IMAGE")) {
			takeScreenshot();

			echo("URL login failed, falling back to UI login.");

			User.firstLoginUI(
				idpName = ${idpName},
				password = ${password},
				rememberMeChecked = ${rememberMeChecked},
				specificURL = ${virtualHostsURL},
				userEmailAddress = ${userEmailAddress},
				userScreenName = ${userScreenName});
		}
		else {
			WaitForLiferayEvent.initializeLiferayEventLog();
		}
	}

	@summary = "Default summary"
	macro firstLoginUI(localization = null, idpName = null, localizedAgree = null, localizedSaveButton = null, samlMultiIdPs = null, userEmailAddress = null, newPassword = null, userId = null, password = null, authenticationMethod = null, domain = null, userScreenName = null, specificURL = null, rememberMeChecked = null, localizedAnswer = null, successful = null) {
		if (isSet(specificURL)) {
			Navigator.openSpecificURL(
				domain = ${domain},
				url = ${specificURL});
		}
		else {
			Navigator.openURL();
		}

		User._clickSignInLink(
			idpName = ${idpName},
			localization = ${localization},
			samlMultiIdPs = ${samlMultiIdPs});

		if (${authenticationMethod} == "By Screen Name") {
			Type(
				locator1 = "TextInput#SCREEN_NAME",
				value1 = ${userScreenName});
		}
		else if (${authenticationMethod} == "By User ID") {
			Type.typePause(
				key_text = "ID",
				locator1 = "TextInput#ANY",
				value1 = ${userId});
		}
		else {
			Type.typePause(
				locator1 = "TextInput#EMAIL_ADDRESS",
				value1 = ${userEmailAddress});
		}

		Type.typePause(
			locator1 = "TextInput#PASSWORD",
			value1 = ${password});

		if (isSet(rememberMeChecked) && (${rememberMeChecked} == "true")) {
			Check(locator1 = "Checkbox#REMEMBER_ME");
		}

		User._clickSignInButton(localization = ${localization});

		if (isSet(localizedAgree)) {
			User.acceptEndUserLicenseAgreementLocalized(localizedAgree = ${localizedAgree});
		}
		else if (IsElementPresent(locator1 = "Button#I_AGREE")) {
			SignIn.agreeToTermsOfUse();
		}

		if (IsElementPresent(locator1 = "TextInput#PASSWORD_2")) {
			User.resetPassword(
				localizedSaveButton = ${localizedSaveButton},
				newPassword = ${newPassword},
				successful = ${successful});
		}

		if (isSet(localizedAnswer)) {
			User.answerPasswordReminderLocalized(
				localizedAnswer = ${localizedAnswer},
				localizedSaveButton = ${localizedSaveButton});
		}
		else if (IsElementPresent(locator1 = "Select#QUESTION")) {
			SignIn.setPasswordReminder();
		}

		WaitForLiferayEvent.initializeLiferayEventLog();
	}

	@summary = "Default summary"
	macro forgotPasswordSendResetLink(userEmailAddress = null) {
		SignInNavigator.gotoForgotPassword();

		Type(
			locator1 = "TextInput#EMAIL_ADDRESS",
			value1 = ${userEmailAddress});

		Button.clickNext();

		AssertClick(
			locator1 = "Button#SEND_PASSWORD_RESET_LINK",
			value1 = "Send Password Reset Link");

		Alert.viewSuccessMessageText(successMessage = "Your request completed successfully.");
	}

	@summary = "Default summary"
	macro gotoAddPage(userName = null) {
		LexiconEntry.gotoEntryMenuItem(
			menuItem = "Manage Pages",
			rowEntry = ${userName});
	}

	@summary = "Default summary"
	macro impersonateCP(siteName = null, userName = null) {
		if (isSet(userName)) {
			var userScreenName = ${userName};
		}

		LexiconEntry.gotoEntryMenuItem(
			menuItem = "Impersonate User",
			rowEntry = ${userScreenName});

		if (isSet(siteName)) {
			SelectWindow(value1 = "title=Home - ${siteName}");
		}
		else {
			SelectWindow.selectPopUp(value1 = "null");
		}

		UserBar.gotoDropdown();
	}

	@summary = "Default summary"
	macro loginByScreenNamePG(password = null, virtualHostsURL = null, userScreenName = null, newPassword = null) {
		JSONUser.agreeToTermsAndAnswerReminderQueryByScreenName(
			creatorPassword = ${password},
			portalURL = ${virtualHostsURL},
			userScreenName = ${userScreenName});

		if (isSet(virtualHostsURL)) {
			Navigator.openSpecificURL(url = "${virtualHostsURL}/web/guest?SM_USER=${userScreenName}");
		}
		else {
			var baseURL = JSONCompany.getPortalURL();

			Navigator.openSpecificURL(url = "${baseURL}/web/guest?SM_USER=${userScreenName}");
		}

		// TODO Remove below macro after password reminder gets completely cleaned out with JSON

		User.resetPassword(newPassword = ${newPassword});

		User.answerPasswordReminder();

		AssertElementPresent(locator1 = "UserBar#USER_AVATAR_IMAGE");

		WaitForLiferayEvent.initializeLiferayEventLog();
	}

	@summary = "Default summary"
	macro loginPG(idpName = null, password = null, virtualHostsURL = null, userScreenName = null, userEmailAddress = null, newPassword = null, rememberMeChecked = null, nodePort = null) {
		JSONUser.agreeToTermsAndAnswerReminderQuery(
			portalURL = ${virtualHostsURL},
			userEmailAddress = ${userEmailAddress});

		if (isSet(virtualHostsURL)) {
			Navigator.openSpecificURL(url = "${virtualHostsURL}/web/guest?SM_USER=${userEmailAddress}");
		}
		else if (isSet(nodePort)) {
			var baseURL = "http://localhost:${nodePort}";

			Navigator.openSpecificURL(url = "${baseURL}/web/guest?SM_USER=${userEmailAddress}");
		}
		else {
			var baseURL = JSONCompany.getPortalURL();

			Navigator.openSpecificURL(url = "${baseURL}/web/guest?SM_USER=${userEmailAddress}");
		}

		// TODO Remove below macro after password reminder gets completely cleaned out with JSON

		User.resetPassword(newPassword = ${newPassword});

		User.answerPasswordReminder();

		if (IsElementNotPresent(locator1 = "UserBar#USER_AVATAR_IMAGE")) {
			takeScreenshot();

			echo("URL login failed, falling back to UI login.");

			User.firstLoginUI(
				idpName = ${idpName},
				password = ${password},
				rememberMeChecked = ${rememberMeChecked},
				specificURL = ${virtualHostsURL},
				userEmailAddress = ${userEmailAddress},
				userScreenName = ${userScreenName});
		}
		else {
			WaitForLiferayEvent.initializeLiferayEventLog();
		}
	}

	@summary = "Default summary"
	macro loginUserInvalidPG(password = null, userEmailAddress = null) {
		Navigator.openURL();

		var count = 0;

		if (!(isSet(loginAttempts))) {
			var loginAttempts = 1;
		}

		while (${count} != ${loginAttempts}) {
			var count = ${count} + 1;

			User.firstLoginUI(
				password = ${password},
				userEmailAddress = ${userEmailAddress});

			AssertTextEquals.assertPartialText(
				locator1 = "Message#ERROR_DISMISSIBLE",
				value1 = "Authentication failed");
		}
	}

	@description = "Log into Liferay Portal as ${userEmailAddress} using the password ${password}."
	@summary = "Default summary"
	macro loginUserPG(password = null, userEmailAddress = null) {
		Navigator.openURL();

		User._login(
			password = ${password},
			userEmailAddress = ${userEmailAddress});

		if (IsElementPresent(locator1 = "UserBar#USER_AVATAR_IN_NAVIGATION_USER_PERSONAL_BAR_PORTLET")) {
			AssertElementPresent(locator1 = "UserBar#USER_AVATAR_IMAGE");
		}
	}

	@description = "Log into Liferay Portal as ${userEmailAddress} using the password ${password} with CAS authentication enabled."
	@summary = "Default summary"
	macro loginUserWithCASEnabledPG(userPassword = null, userScreenName = null) {
		Navigator.openURL();

		Navigator.gotoLoginPage();

		Type(
			locator1 = "CAS#NETID_FIELD",
			value1 = ${userScreenName});

		Type(
			locator1 = "CAS#PASSWORD_FIELD",
			value1 = ${userPassword});

		AssertClick(
			locator1 = "CAS#LOGIN_BUTTON",
			value1 = "LOGIN");

		AssertElementPresent(locator1 = "UserBar#USER_AVATAR_IMAGE");
	}

	@description = "Log into Facebook as ${facebookEmail} using the password ${facebookPassword}."
	@summary = "Default summary"
	macro loginUserWithFacebookSSOEnabledPG(facebookPassword = null, newPassword = null, facebookEmail = null) {
		Navigator.openURL();

		Navigator.gotoLoginPage();

		AssertClick(
			locator1 = "Link#FACEBOOK",
			value1 = "Facebook");

		SelectWindow(value1 = "title=Log into Facebook | Facebook");

		Type(
			locator1 = "FacebookLogin#LOGIN_EMAIL_FIELD",
			value1 = ${facebookEmail});

		Type(
			locator1 = "FacebookLogin#LOGIN_PASSWORD_FIELD",
			value1 = ${facebookPassword});

		Click(locator1 = "FacebookLogin#LOGIN_BUTTON");

		Alert.confirmSecurity();

		SelectWindow.selectWindowTopNoSPARefresh();

		if (IsElementPresent.pauseIsElementPresent(locator1 = "TextInput#EMAIL_ADDRESS")) {
			if (IsElementPresent(locator1 = "Button#SUBMIT")) {
				Click(locator1 = "Button#SUBMIT");
			}

			SelectWindow.selectWindowNoSPARefresh(locator1 = "title=Home - Liferay DXP");

			if (IsElementPresent.pauseIsElementPresent(locator1 = "Button#I_AGREE", value1 = "I Agree")) {
				AssertClick(
					locator1 = "Button#I_AGREE",
					value1 = "I Agree");
			}

			User.resetPassword(newPassword = ${newPassword});

			if (IsElementPresent.pauseIsElementPresent(locator1 = "TextInput#ANSWER")) {
				Type(
					locator1 = "TextInput#ANSWER",
					value1 = "test");

				ClickNoError(locator1 = "Button#SAVE");
			}
		}

		SelectWindow.selectWindowTopNoSPARefresh();

		AssertElementPresent(locator1 = "UserBar#USER_AVATAR_IMAGE");
	}

	@summary = "Default summary"
	macro loginUserWithGoogleSSOEnabledPG(userPassword = null, userEmailAddress = null) {
		GoogleLogin.accountSignIn(
			userEmailAddress = ${userEmailAddress},
			userPassword = ${userPassword});

		GoogleLogin.acceptConditions();

		AssertElementPresent(locator1 = "UserBar#USER_AVATAR_IMAGE");
	}

	@summary = "Default summary"
	macro loginUserWithOpenIDConnectEnabledPG(userPassword = null, specificURL = null, userEmailAddress = null) {
		OpenIDConnect.accountSignIn(
			specificURL = ${specificURL},
			userEmailAddress = ${userEmailAddress},
			userPassword = ${userPassword});

		OpenIDConnect.acceptConditions();

		OpenIDConnect.acceptConditions();

		AssertElementPresent(locator1 = "UserBar#USER_AVATAR_IMAGE");
	}

	@summary = "Default summary"
	macro logoutAndLoginPG(password = null, authenticationMethod = null, userScreenName = null, userLoginEmailAddress = null, nodePort = null, signOutLocalizationName = null) {
		Navigator.openURL();

		UserBar.signOut(signOutLocalizationName = ${signOutLocalizationName});

		User.loginPG(
			authenticationMethod = ${authenticationMethod},
			nodePort = ${nodePort},
			password = ${password},
			userEmailAddress = ${userLoginEmailAddress},
			userScreenName = ${userScreenName});
	}

	@description = "Log out of Liferay Portal."
	@summary = "Default summary"
	macro logoutPG(specificURL = null, signOutLocalizationName = null) {
		if (isSet(specificURL)) {
			Navigator.openSpecificURL(url = ${specificURL});
		}
		else {
			Navigator.openURL();
		}

		UserBar.signOut(signOutLocalizationName = ${signOutLocalizationName});
	}

	@description = "Log out of Liferay Portal via URL."
	@summary = "Default summary"
	macro logoutViaURL() {
		var baseURL = JSONCompany.getPortalURL();

		Open(value1 = "${baseURL}/c/portal/logout");
	}

	// This new macro is created after LPS-168612. Once Contact Information are fixed the same way as LPS-168612, then we can consolidate this macro and User.makePrimary

	@summary = "Default summary"
	macro makeAddressPrimary(listEntry = null) {
		Click.waitForMenuToggleJSClick(
			key_listEntry = ${listEntry},
			locator1 = "LexiconList#LIST_ENTRY_ELLIPSIS");

		MenuItem.click(menuItem = "Make Primary");
	}

	@summary = "Default summary"
	macro makePrimary(rowEntry = null) {
		Click.waitForMenuToggleJSClick(
			key_rowEntry = ${rowEntry},
			locator1 = "Icon#TABLE_VERTICAL_ELLIPSIS");

		MenuItem.click(menuItem = "Make Primary");
	}

	@summary = "Default summary"
	macro openUsersAdmin(baseURL = null) {
		Navigator.openWithAppendToBaseURL(
			baseURL = ${baseURL},
			urlAppend = "group/control_panel/manage?p_p_id=com_liferay_users_admin_web_portlet_UsersAdminPortlet");
	}

	// This new macro is created after LPS-168612. Once Contact Information are fixed the same way as LPS-168612, then we can consolidate this macro and User.removeContactInformation

	@summary = "Default summary"
	macro removeAddress(listEntry = null) {
		Click.waitForMenuToggleJSClick(
			key_listEntry = ${listEntry},
			locator1 = "LexiconList#LIST_ENTRY_ELLIPSIS");

		MenuItem.click(menuItem = "Remove");
	}

	@summary = "Default summary"
	macro removeContactInformation(rowEntry = null) {
		Click.waitForMenuToggleJSClick(
			key_rowEntry = ${rowEntry},
			locator1 = "Icon#TABLE_VERTICAL_ELLIPSIS");

		MenuItem.click(menuItem = "Remove");
	}

	@summary = "Default summary"
	macro RequestPasswordResetLink(userEmailAddress = null) {
		ACUtils.clickGenericHyperlink(hyperlinkText = "Request a new password reset link.");

		Type(
			locator1 = "TextInput#EMAIL_ADDRESS",
			value1 = ${userEmailAddress});

		Button.clickNext();

		AssertClick(
			locator1 = "Button#SEND_PASSWORD_RESET_LINK",
			value1 = "Send Password Reset Link");

		Alert.viewSuccessMessageText(successMessage = "Your request completed successfully.");
	}

	@summary = "Default summary"
	macro resetPassword(localizedSaveButton = null, newPassword = null, successful = null) {
		if (IsElementPresent.pauseIsElementPresent(locator1 = "TextInput#PASSWORD")) {
			if (isSet(newPassword)) {
				User.fillResetPasswordFields(password = ${newPassword});
			}
			else {
				User.fillResetPasswordFields(password = PropsUtil.get("default.admin.password"));
			}

			if (isSet(localizedSaveButton)) {
				Button.click(button = ${localizedSaveButton});
			}
			else if (isSet(successful) && (${successful} == "true")) {
				Button.clickSubmitButton();

				AssertElementNotPresent(locator1 = "TextInput#REENTER_PASSWORD");
			}
			else {
				Button.clickSubmitButton();
			}
		}
	}

	@summary = "Default summary"
	macro searchCP(searchTerm = null) {
		Search.searchCP(searchTerm = ${searchTerm});

		AssertElementPresent(locator1 = "Search#SEARCH_INFO");
	}

	@summary = "Default summary"
	macro selectMembership(assetType = null) {
		UserNavigator.gotoMemberships();

		if (${assetType} == "Sites") {
			var key_selectId = "selectSiteLink";
		}
		else if (${assetType} == "User Groups") {
			var key_selectId = "UserGroupsLink";
		}
		else if (${assetType} == "Accounts") {
			var key_selectId = "selectAccountLink";
		}
		else if (${assetType} == "Asset Libraries") {
			var key_selectId = "selectDepotGroupLink";
		}

		AssertClick(
			locator1 = "UsersAndOrganizationsEditUser#MEMBERSHIPS_SELECT_ASSET_BUTTON",
			value1 = "Select");
	}

	@summary = "Default summary"
	macro selectUserTagCP(tagNameList = null) {
		Click(
			key_fieldLabel = "Tags",
			key_text = "Select",
			locator1 = "Button#ANY_WITH_LABEL");

		IFrame.selectTagFrame();

		for (var tagName : list ${tagNameList}) {
			Check(
				key_rowEntry = ${tagName},
				locator1 = "ContentRow#ENTRY_CONTENT_ENTRY_CHECKBOX");
		}

		SelectFrameTop();

		AssertClick(
			locator1 = "Button#DONE",
			value1 = "Done");

		for (var tagName : list ${tagNameList}) {
			AssertTextEquals(
				key_tagName = ${tagName},
				locator1 = "AssetCategorization#TAG",
				value1 = ${tagName});
		}
	}

	@summary = "Default summary"
	macro setPasswordByResetLink(newPassword1 = null, newPassword2 = null) {
		Type(
			locator1 = "UsersAndOrganizationsEditUser#PASSWORD_NEW_PASSWORD_FIELD",
			value1 = ${newPassword1});

		Type(
			locator1 = "UsersAndOrganizationsEditUser#PASSWORD_ENTER_AGAIN_FIELD",
			value1 = ${newPassword2});

		Button.clickSave();

		User.acceptEndUserLicenseAgreement();

		User.answerPasswordReminder();
	}

	@summary = "Default summary"
	macro signInPG(localization = null, idpName = null, emailVerification = null, localizedAgree = null, localizedSaveButton = null, samlMultiIdPs = null, userEmailAddress = null, newPassword = null, userId = null, password = null, authenticationMethod = null, userScreenName = null, rememberMeChecked = null, localizedAnswer = null) {
		User._clickSignInLink(
			idpName = ${idpName},
			localization = ${localization},
			samlMultiIdPs = ${samlMultiIdPs});

		if (${authenticationMethod} == "By Screen Name") {
			Type(
				locator1 = "TextInput#SCREEN_NAME",
				value1 = ${userScreenName});
		}
		else if (${authenticationMethod} == "By User ID") {
			Type.typePause(
				key_text = "ID",
				locator1 = "TextInput#ANY",
				value1 = ${userId});
		}
		else {
			Type.typePause(
				locator1 = "TextInput#EMAIL_ADDRESS",
				value1 = ${userEmailAddress});
		}

		Type.typePause(
			locator1 = "TextInput#PASSWORD",
			value1 = ${password});

		if (isSet(rememberMeChecked) && (${rememberMeChecked} == "true")) {
			Check(locator1 = "Checkbox#REMEMBER_ME");
		}

		User._clickSignInButton(localization = ${localization});

		if (isSet(localizedAgree)) {
			User.acceptEndUserLicenseAgreementLocalized(localizedAgree = ${localizedAgree});
		}
		else if (IsElementPresent(locator1 = "Button#I_AGREE")) {
			SignIn.agreeToTermsOfUse();
		}

		if (isSet(emailVerification)) {
			AssertTextEquals(
				locator1 = "Message#INFO",
				value1 = "Please enter your verification code.");

			Type(
				locator1 = "SignInCreateAccount#EMAIL_VERIFICATION_CODE_FIELD",
				value1 = ${emailVerification});

			AssertClick(
				locator1 = "Button#VERIFY",
				value1 = "Verify");
		}

		User.resetPassword(newPassword = ${newPassword});

		if (isSet(localizedAnswer)) {
			User.answerPasswordReminderLocalized(
				localizedAnswer = ${localizedAnswer},
				localizedSaveButton = ${localizedSaveButton});
		}
		else if (IsElementPresent(locator1 = "Select#QUESTION")) {
			SignIn.setPasswordReminder();
		}

		AssertVisible(locator1 = "UserBar#USER_AVATAR_IMAGE");

		WaitForLiferayEvent.initializeLiferayEventLog();
	}

	@summary = "Default summary"
	macro singleDeactivateCP(userFirstName = null) {
		LexiconEntry.gotoEntryMenuItemNoError(
			menuItem = "Deactivate",
			rowEntry = ${userFirstName});

		AssertConfirm(value1 = "Are you sure you want to deactivate this?");

		Alert.viewSuccessMessage();
	}

	@summary = "Default summary"
	macro singleDeleteCP(userFirstName = null) {
		LexiconEntry.gotoEntryMenuItemNoError(
			menuItem = "Delete",
			rowEntry = ${userFirstName});

		AssertConfirm(value1 = "Are you sure you want to delete this? It will be deleted immediately.");

		Alert.viewSuccessMessage();
	}

	@summary = "Default summary"
	macro tearDownCP() {
		User.openUsersAdmin();

		ManagementBar.setFilterAndOrder(filterBy = "Active");

		while (IsElementPresent(key_rowNumber = 2, locator1 = "ContentRow#ENTRY_CONTENT_ROW_NUMBER")) {
			Click.waitForMenuToggleJSClick(
				key_rowNumber = 2,
				locator1 = "ContentRow#ENTRY_CONTENT_ROW_NUMBER_ELLIPSIS");

			MenuItem.clickNoError(menuItem = "Deactivate");

			AssertConfirm(value1 = "Are you sure you want to deactivate this?");
		}

		ManagementBar.setFilterAndOrder(filterBy = "Inactive");

		if (IsElementPresent(locator1 = "ContentRow#ENTRY_CONTENT_ROW_1")) {
			PortletEntry.selectAll();

			ClickNoError(
				key_text = "trash",
				locator1 = "ManagementBar#ANY_ICON");

			AssertConfirm(value1 = "Are you sure you want to permanently delete the selected users?");

			Alert.viewSuccessMessage();
		}

		AssertElementPresent(locator1 = "Message#EMPTY_INFO");
	}

	@summary = "Default summary"
	macro tearDownSpecificUser(userScreenName = null) {
		User.openUsersAdmin();

		ManagementBar.setFilterAndOrder(filterBy = "Active");

		User.searchCP(searchTerm = ${userScreenName});

		if (IsElementPresent(locator1 = "ContentRow#ENTRY_CONTENT_ROW_1")) {
			PortletEntry.selectAll();

			ClickNoError(locator1 = "Icon#MOVE_TO_RECYCLE_BIN");

			AssertConfirm(value1 = "Are you sure you want to deactivate the selected users?");

			Alert.viewSuccessMessage();
		}

		ManagementBar.setFilterAndOrder(filterBy = "Inactive");

		if (IsElementPresent(locator1 = "ContentRow#ENTRY_CONTENT_ROW_1")) {
			PortletEntry.selectAll();

			ClickNoError(locator1 = "Icon#MOVE_TO_RECYCLE_BIN");

			AssertConfirm(value1 = "Are you sure you want to permanently delete the selected users?");

			Alert.viewSuccessMessage();
		}
	}

	@summary = "Default summary"
	macro unassignMembershipCP(userScreenName = null) {
		User.openUsersAdmin();

		UserNavigator.gotoUser(userScreenName = ${userScreenName});

		UserNavigator.gotoMemberships();

		for (var entryName : list ${entryName}) {
			Click(
				key_entryName = ${entryName},
				locator1 = "LexiconTable#TABLE_ENTRY_DELETE");
		}

		Button.clickSave();

		Alert.viewSuccessMessage();
	}

	@summary = "Default summary"
	macro unassignRegularRoleCP(userScreenName = null, regularRoleName = null) {
		UserNavigator.gotoUser(userScreenName = ${userScreenName});

		UserNavigator.gotoRoles();

		var key_regularRoleName = ${regularRoleName};

		Click(
			key_entryName = ${regularRoleName},
			locator1 = "LexiconTable#TABLE_ENTRY_DELETE");

		PortletEntry.save();

		AssertElementNotPresent(
			locator1 = "UsersAndOrganizationsEditUser#ROLES_REGULAR_ROLES_TABLE_TITLE",
			value1 = ${regularRoleName});
	}

	@summary = "Default summary"
	macro unlockUserAccount(userScreenName = null) {
		var key_userScreenName = ${userScreenName};

		AssertTextEquals(
			locator1 = "UsersAndOrganizations#USER_TABLE_SCREEN_NAME",
			value1 = ${userScreenName});

		AssertClick(
			locator1 = "UsersAndOrganizations#USER_TABLE_SCREEN_NAME",
			value1 = ${userScreenName});

		AssertTextEquals(
			locator1 = "Message#ERROR_3",
			value1 = "Warning:This user account has been locked due to excessive failed login attempts.");

		AssertClick(
			locator1 = "Button#UNLOCK",
			value1 = "Unlock");

		Alert.viewSuccessMessage();
	}

	@summary = "Default summary"
	macro verifyEmail(portalURL = null, recipient = null) {
		if (IsElementPresent.pauseIsElementPresent(locator1 = "TextInput#EMAIL_VERIFICATION_CODE")) {
			var bodySpecificText = MockMock.getBodySpecificText(
				asset = "${portalURL}: Email Address Verification",
				content = "Please verify your email address for ${portalURL}",
				extractFirstText = "&",
				recipient = ${recipient},
				sender = "Joe Bloggs",
				specificText = "ticketKey=",
				subject = "${portalURL}: Email Address Verification");

			Navigator.openURL();

			Type(
				locator1 = "TextInput#EMAIL_VERIFICATION_CODE",
				value1 = ${bodySpecificText});

			Button.clickVerifyButton();

			AssertElementNotPresent(locator1 = "TextInput#EMAIL_VERIFICATION_CODE");
		}
	}

	@summary = "Default summary"
	macro viewAddress(country = null, city = null, addressType = null, postalCode = null, street1 = null, region = null) {
		var key_primaryValue = ${street1};

		AssertTextEquals.assertPartialText(
			locator1 = "UsersAndOrganizations#USER_ADDRESS_TABLE",
			value1 = ${city});

		AssertTextEquals.assertPartialText(
			locator1 = "UsersAndOrganizations#USER_ADDRESS_TABLE",
			value1 = ${country});

		AssertTextEquals.assertPartialText(
			locator1 = "UsersAndOrganizations#USER_ADDRESS_TABLE",
			value1 = ${postalCode});

		AssertTextEquals.assertPartialText(
			locator1 = "UsersAndOrganizations#USER_ADDRESS_TABLE",
			value1 = ${region});

		AssertTextEquals.assertPartialText(
			locator1 = "UsersAndOrganizations#USER_ADDRESS_TABLE",
			value1 = ${street1});

		if (isSet(addressType)) {
			AssertTextEquals.assertPartialText(
				locator1 = "UsersAndOrganizations#USER_ADDRESS_TABLE_ADDRESS_TYPE",
				value1 = ${addressType});
		}
	}

	@summary = "Default summary"
	macro viewCaptchaPG(captchaType = null) {
		if (${captchaType} == "captcha") {
			AssertElementPresent.assertElementPresent(locator1 = "SignInCreateAccount#CAPTCHA_IMAGE");
		}
		else if (${captchaType} == "recaptcha") {
			SelectFrame.selectFrameNoLoading(locator1 = "IFrame#RECAPTCHA");

			AssertElementPresent.assertElementPresent(locator1 = "SignInCreateAccount#RECAPTCHA_LABEL");
		}
	}

	@summary = "Default summary"
	macro viewContactInformation(number = null, skype = null, extension = null, numberType = null, address = null, addressPrefix = null, searchContainerType = null, url = null, addressCount = null, websiteType = null) {
		if (isSet(searchContainerType)) {
			var key_searchContainerType = ${searchContainerType};

			// Phone Numbers

			if (isSet(number)) {
				AssertTextEquals.assertPartialText(
					locator1 = "UsersAndOrganizations#USER_CONTACT_INFORMATION_PHONE_NUMBER_TABLE",
					value1 = ${number});
			}

			if (isSet(numberType)) {
				AssertTextEquals.assertPartialText(
					locator1 = "UsersAndOrganizations#USER_CONTACT_INFORMATION_TYPE_TABLE",
					value1 = ${numberType});
			}

			if (isSet(extension)) {
				AssertTextEquals.assertPartialText(
					locator1 = "UsersAndOrganizations#USER_CONTACT_INFORMATION_EXTENSION_TABLE",
					value1 = ${extension});
			}

			// Additional Email Addresses

			if (isSet(address)) {
				AssertTextEquals.assertPartialText(
					locator1 = "UsersAndOrganizations#USER_CONTACT_INFORMATION_EMAIL_ADDRESS_TABLE",
					value1 = ${address});
			}

			if (isSet(addressCount)) {
				var i = 0;

				while (${i} != ${addressCount}) {
					var i = ${i} + 1;

					AssertTextEquals.assertPartialText(
						locator1 = "UsersAndOrganizations#USER_CONTACT_INFORMATION_CONTAINER_TABLE",
						value1 = "${addressPrefix}${i}@liferay.com");
				}
			}

			// Websites

			if (isSet(url)) {
				AssertTextEquals.assertPartialText(
					locator1 = "UsersAndOrganizations#USER_CONTACT_INFORMATION_EMAIL_ADDRESS_TABLE",
					value1 = ${url});
			}

			if (isSet(websiteType)) {
				AssertTextEquals.assertPartialText(
					locator1 = "UsersAndOrganizations#USER_CONTACT_INFORMATION_TYPE_TABLE",
					value1 = ${websiteType});
			}
		}

		// Instant Messenger

		if (isSet(skype)) {
			AssertTextEquals.assertValue(
				locator1 = "TextInput#SKYPE",
				value1 = ${skype});
		}
	}

	@summary = "Default summary"
	macro viewCP(userEmailAddressVariation = null, userScreenNameVariation = null, userLastName = null, userScreenName = null, userEmailAddress = null, userFirstName = null) {
		if (${userEmailAddressVariation} == "At") {
			Search.searchCP(searchTerm = ${userEmailAddress});

			AssertTextEquals.assertPartialText(
				locator1 = "Message#EMPTY_INFO",
				value1 = "No users were found.");
		}
		else if (${userEmailAddressVariation} == "Period") {
			Search.searchCP(searchTerm = ${userEmailAddress});

			AssertTextEquals.assertPartialText(
				locator1 = "Message#EMPTY_INFO",
				value1 = "No users were found.");
		}
		else if (${userScreenNameVariation} == "Invalid") {
			Search.searchCP(searchTerm = ${userScreenName});

			AssertTextEquals.assertPartialText(
				locator1 = "Message#EMPTY_INFO",
				value1 = "No users were found.");
		}
		else {
			UserNavigator.gotoUser(userScreenName = ${userScreenName});

			User.viewUserInformation(
				userEmailAddress = ${userEmailAddress},
				userFirstName = ${userFirstName},
				userLastName = ${userLastName},
				userScreenName = ${userScreenName});
		}
	}

	@summary = "Default summary"
	macro viewExpiredSessionErrorMessage(message = null) {
		AssertTextEquals.assertPartialText(
			locator1 = "Home#ERROR_MESSAGE_EXPIRED_SESSION",
			value1 = ${message});
	}

	@summary = "Default summary"
	macro viewIsPrimary(primaryValue = null, searchContainerType = null, contactInfoPrimaryValue = null) {
		if (isSet(primaryValue)) {
			var key_primaryValue = ${primaryValue};

			AssertElementPresent(locator1 = "UsersAndOrganizations#USER_ADDRESS_PRIMARY_TABLE");
		}

		if (isSet(searchContainerType)) {
			var key_contactInfoPrimaryValue = ${contactInfoPrimaryValue};
			var key_searchContainerType = ${searchContainerType};

			AssertElementPresent(locator1 = "UsersAndOrganizations#USER_CONTACT_INFORMATION_PRIMARY_TABLE");
		}
	}

	@summary = "Default summary"
	macro viewLoggedInPG(authenticationMethod = null) {
		if (${authenticationMethod} == "By Screen Name") {
			AssertElementNotPresent(locator1 = "TextInput#SCREEN_NAME");
		}
		else {
			AssertElementNotPresent(locator1 = "TextInput#EMAIL_ADDRESS");
		}

		AssertElementNotPresent(locator1 = "TextInput#PASSWORD");

		AssertElementNotPresent(locator1 = "Checkbox#REMEMBER_ME");

		AssertElementNotPresent(locator1 = "Button#SIGN_IN");

		AssertVisible(locator1 = "UserBar#USER_AVATAR_IMAGE");
	}

	@summary = "Default summary"
	macro viewLoggedOutPG() {
		AssertElementNotPresent(locator1 = "UserBar#USER_AVATAR_IMAGE");

		AssertElementNotPresent(locator1 = "ControlMenu#ADD");

		AssertTextEquals(
			locator1 = "UserBar#USER_SIGN_IN",
			value1 = "Sign In");
	}

	@summary = "Default summary"
	macro viewLoggedOutWithCASEnabledPG() {
		AssertTextEquals(
			locator1 = "CAS#APP_NAME_HEADER",
			value1 = "Central Authentication Service (CAS)");

		AssertTextEquals(
			locator1 = "CAS#LOGOUT_SUCCESSFUL_HEADER",
			value1 = "Logout successful");

		AssertTextEquals(
			locator1 = "CAS#LOGOUT_SUCCESSFUL_MESSAGE_1",
			value1 = "You have successfully logged out of the Central Authentication Service.");

		AssertTextEquals(
			locator1 = "CAS#LOGOUT_SUCCESSFUL_MESSAGE_2",
			value1 = "For security reasons, exit your web browser.");
	}

	@summary = "Default summary"
	macro viewLoginPG(authenticationMethod = null, loginPg = null) {
		if (${authenticationMethod} == "By Screen Name") {
			AssertElementPresent(locator1 = "TextInput#SCREEN_NAME");
		}
		else {
			AssertElementPresent(locator1 = "TextInput#EMAIL_ADDRESS");
		}

		AssertElementPresent(locator1 = "TextInput#PASSWORD");

		AssertElementPresent(locator1 = "Checkbox#REMEMBER_ME");

		if (${loginPg} == "Full Screen") {
			AssertElementNotPresent(locator1 = "UserBar#USER_SIGN_IN");
		}
		else {
			AssertTextEquals(
				locator1 = "Button#SIGN_IN",
				value1 = "Sign In");
		}
	}

	@summary = "Default summary"
	macro viewMemberships(assigneeName = null, roleName = null, assetType = null) {
		UserNavigator.gotoMemberships();

		if (${assetType} == "Sites") {
			var key_assetType = "groups";
		}
		else if (${assetType} == "User Groups") {
			var key_assetType = "userGroups";
		}
		else if (${assetType} == "Accounts") {
			var key_assetType = "accountEntries";
		}
		else if (${assetType} == "Asset Libraries") {
			var key_assetType = "depotGroups";
		}

		var key_assigneeName = ${assigneeName};

		AssertTextEquals(
			locator1 = "UsersAndOrganizationsEditUser#MEMBERSHIPS_TABLE_ENTRY_NAME",
			value1 = ${assigneeName});

		if (isSet(roleName)) {
			var key_assigneeName = ${assigneeName};
			var key_roleName = ${roleName};

			AssertElementPresent(
				locator1 = "UsersAndOrganizationsEditUser#MEMBERSHIPS_TABLE_ENTRY_ROLE",
				value1 = ${roleName});
		}
	}

	@summary = "Default summary"
	macro viewNoPasswordSection(specificURL = null) {
		if (isSet(specificURL)) {
			Navigator.openSpecificURL(url = ${specificURL});
		}
		else {
			Navigator.openURL();
		}

		UserBar.signOut();

		SignInNavigator.gotoCreateAccount();

		AssertElementNotPresent(locator1 = "TextInput#PASSWORD");

		AssertElementNotPresent(locator1 = "TextInput#REENTER_PASSWORD");
	}

	@summary = "Default summary"
	macro viewNotPrimary(primaryValue = null, searchContainerType = null, contactInfoPrimaryValue = null) {
		if (isSet(primaryValue)) {
			var key_primaryValue = ${primaryValue};

			AssertElementNotPresent(locator1 = "UsersAndOrganizations#USER_ADDRESS_PRIMARY_TABLE");
		}

		if (isSet(searchContainerType)) {
			var key_contactInfoPrimaryValue = ${contactInfoPrimaryValue};
			var key_searchContainerType = ${searchContainerType};

			AssertElementNotPresent(locator1 = "UsersAndOrganizations#USER_CONTACT_INFORMATION_PRIMARY_TABLE");
		}
	}

	@summary = "Default summary"
	macro viewNoUsersFoundCP() {
		AssertTextEquals.assertPartialText(
			locator1 = "Message#EMPTY_INFO",
			value1 = "No users were found.");
	}

	@summary = "Default summary"
	macro viewPasswordSection(specificURL = null) {
		if (isSet(specificURL)) {
			Navigator.openSpecificURL(url = ${specificURL});
		}
		else {
			Navigator.openURL();
		}

		UserBar.signOut();

		SignInNavigator.gotoCreateAccount();

		AssertElementPresent(locator1 = "TextInput#PASSWORD");

		AssertElementPresent(locator1 = "TextInput#REENTER_PASSWORD");
	}

	@summary = "Default summary"
	macro viewSessionNotExpired() {
		AssertElementNotPresent(locator1 = "Home#ERROR_MESSAGE_EXPIRED_SESSION");

		if (!(isSet(noSignin))) {
			AssertElementNotPresent(locator1 = "UserBar#USER_SIGN_IN");
		}
	}

	@summary = "Default summary"
	macro viewSignIn() {
		AssertElementPresent(locator1 = "UserBar#USER_SIGN_IN");
	}

	@summary = "Default summary"
	macro viewSignInPresent() {
		AssertElementPresent(locator1 = "UserBar#USER_SIGN_IN");
	}

	@summary = "Default summary"
	macro viewUnableToAssignRoleToUserCP(roleType = null) {
		AssertElementNotPresent(
			key_roleType = StringUtil.replace(${roleType}, " ", ""),
			locator1 = "UsersAndOrganizationsEditUser#ROLES_SELECT_LINK",
			value1 = "Select");

		if (contains(${roleType}, "Site")) {
			var emptyMessage = "This user does not belong to a site to which a site role can be assigned.";
		}

		if (contains(${roleType}, "Organization")) {
			var emptyMessage = "This user does not belong to an organization to which an organization role can be assigned.";
		}

		AssertTextEquals(
			key_roleType = ${roleType},
			locator1 = "Message#ROLE_EMPTY_MUTED_INFO",
			value1 = ${emptyMessage});
	}

	@summary = "Default summary"
	macro viewUserCustomFieldsCP(customFieldName = null, customFieldType = null, startingValue = null) {
		var key_customFieldName = ${customFieldName};

		AssertTextEquals(
			locator1 = "UsersAndOrganizationsEditUser#CUSTOM_FIELDS_LABEL",
			value1 = ${customFieldName});

		if (isSet(startingValue)) {
			if (${customFieldType} == "Text Area") {
				AssertTextEquals(
					locator1 = "TextInput#CUSTOM_FIELD",
					value1 = ${startingValue});
			}
			else if (${customFieldType} == "Text Field") {
				AssertTextEquals(
					key_fieldLabel = ${customFieldName},
					locator1 = "TextInput#GENERIC_TEXT_INPUT",
					value1 = ${startingValue});
			}
		}
	}

	@summary = "Default summary"
	macro viewUserInformation(userJobTitle = null, userLastName = null, userScreenName = null, userMiddleName = null, userEmailAddress = null, userFirstName = null, userId = null, userBirthday = null) {
		AssertTextEquals(
			locator1 = "UsersAndOrganizationsEditUser#DETAILS_SCREEN_NAME_FIELD",
			value1 = ${userScreenName});

		AssertTextEquals(
			locator1 = "UsersAndOrganizationsEditUser#DETAILS_EMAIL_ADDRESS_FIELD",
			value1 = ${userEmailAddress});

		AssertTextEquals(
			locator1 = "UsersAndOrganizationsEditUser#DETAILS_FIRST_NAME_FIELD",
			value1 = ${userFirstName});

		AssertTextEquals(
			locator1 = "UsersAndOrganizationsEditUser#DETAILS_LAST_NAME_FIELD",
			value1 = ${userLastName});

		if (isSet(userBirthday)) {
			AssertTextEquals(
				locator1 = "UsersAndOrganizationsEditUser#DETAILS_BIRTHDAY_FIELD",
				value1 = ${userBirthday});
		}

		if (isSet(userMiddleName)) {
			AssertTextEquals(
				locator1 = "UsersAndOrganizationsEditUser#DETAILS_MIDDLE_NAME_FIELD",
				value1 = ${userMiddleName});
		}

		if (isSet(userJobTitle)) {
			AssertTextEquals(
				locator1 = "UsersAndOrganizationsEditUser#DETAILS_JOB_TITLE_FIELD",
				value1 = ${userJobTitle});
		}

		if (isSet(userId)) {
			AssertTextEquals(
				locator1 = "UsersAndOrganizationsEditUser#DETAILS_SCREEN_NAME_FIELD",
				value1 = ${userId});
		}
	}

	@summary = "Default summary"
	macro viewUserInformationRolesCP(roleTitle = null, roleType = null) {
		UserNavigator.gotoRoles();

		var key_roleTitle = ${roleTitle};

		if (${roleType} == "Inherited Site Roles") {
			AssertTextEquals(
				locator1 = "UsersAndOrganizationsEditUser#ROLES_INHERITED_SITE_ROLES_TABLE_TITLE",
				value1 = ${roleTitle});
		}
		else {
			AssertTextEquals(
				locator1 = "UsersAndOrganizationsEditUser#ROLES_SELECT_ROLE_NAME",
				value1 = ${roleTitle});
		}
	}

	@summary = "Default summary"
	macro viewUserTableName(key_userScreenName = null, userln = null, userfn = null) {
		AssertTextEquals(
			key_userScreenName = ${key_userScreenName},
			locator1 = "UsersAndOrganizations#USER_TABLE_NAME",
			value1 = "${userfn} ${userln}");
	}

	@summary = "Default summary"
	macro viewWelcomeMEssageHTML(userln = null, userfn = null) {
		AssertHTMLSourceTextPresent(
			locator1 = "JSONWS#SECURITY_RESULT",
			value1 = "Welcome ${userfn} ${userln}!");
	}

}