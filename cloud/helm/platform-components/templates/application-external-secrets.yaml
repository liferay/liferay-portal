{{- $operatorApplication := dict
    "name" "external-secrets"
    "syncWave" "-100"
-}}
{{- include "liferay-platform.operatorApplication" (list . (merge $operatorApplication .Values.operatorApplications.externalSecrets)) }}