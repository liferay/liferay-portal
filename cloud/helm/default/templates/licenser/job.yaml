{{- if .Values.subscription.enabled -}}
apiVersion: batch/v1
kind: Job
metadata:
    labels:
        {{- include "liferay.labels" . | nindent 8 }}
    name: "{{ include "liferay.name" . }}-licenser-init"
    namespace: {{ include "liferay.namespace" . }}
spec:
    template:
        spec:
            {{- include "liferay.licenser.jobSpec" . | nindent 12 }}
{{- end -}}
