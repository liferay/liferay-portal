suite: Certificate ExternalSecret
templates:
    -   templates/externalsecret-certificate.yaml
tests:
    -   asserts:
            -   equal:
                    path: metadata.name
                    value: web-cert
            -   equal:
                    path: spec.dataFrom[0].extract.key
                    value: liferay-certificates-web-cert
            -   equal:
                    path: spec.secretStoreRef.name
                    value: my-store
            -   hasDocuments:
                    count: 1
            -   isKind:
                    of: ExternalSecret
        it: Creates a certificate ExternalSecret keyed under the certificates path
        set:
            enabled: true
            endpoints:
                web:
                    port: 443
                    protocol: HTTPS
                    tls:
                        externalSecretNames:
                            -   web-cert
            secretStoreName: my-store
    -   asserts:
            -   hasDocuments:
                    count: 2
        it: Creates one ExternalSecret per distinct certificate across endpoints
        set:
            enabled: true
            endpoints:
                admin:
                    port: 8443
                    protocol: HTTPS
                    tls:
                        externalSecretNames:
                            -   admin-cert
                web:
                    port: 443
                    protocol: HTTPS
                    tls:
                        externalSecretNames:
                            -   web-cert
            secretStoreName: my-store
    -   asserts:
            -   hasDocuments:
                    count: 1
        it: Deduplicates a certificate referenced by more than one endpoint
        set:
            enabled: true
            endpoints:
                admin:
                    port: 8443
                    protocol: HTTPS
                    tls:
                        externalSecretNames:
                            -   shared-cert
                web:
                    port: 443
                    protocol: HTTPS
                    tls:
                        externalSecretNames:
                            -   shared-cert
            secretStoreName: my-store
    -   asserts:
            -   failedTemplate:
                    errorPattern: Secret name must not end with a hyphen and six characters
        it: Fails when a certificate secret name ends like an ARN suffix
        set:
            enabled: true
            endpoints:
                web:
                    port: 443
                    protocol: HTTPS
                    tls:
                        externalSecretNames:
                            -   prod-abc123
            secretStoreName: my-store
    -   asserts:
            -   hasDocuments:
                    count: 0
        it: Omits the certificate ExternalSecret when no secret store is configured
        set:
            enabled: true
            endpoints:
                web:
                    port: 443
                    protocol: HTTPS
                    tls:
                        externalSecretNames:
                            -   web-cert