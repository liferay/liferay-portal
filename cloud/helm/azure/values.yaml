liferay-default:
    autoscaling:
        keda:
            triggerAuthentication:
                spec:
                    podIdentity:
                        provider: azure-workload
        type: keda