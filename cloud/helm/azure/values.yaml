global:
    azure:
        prometheusWorkspaceEndpoint: ""
liferay-default:
    autoscaling:
        keda:
            prometheusServerAddress: "{{ .Values.global.azure.prometheusWorkspaceEndpoint }}"
            triggerAuthentication:
                spec:
                    podIdentity:
                        provider: azure-workload
        type: keda