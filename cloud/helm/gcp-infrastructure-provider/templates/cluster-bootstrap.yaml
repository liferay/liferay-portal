{{- if .Values.clusterBootstrap.enabled -}}
apiVersion: apps/v1
kind: DaemonSet
metadata:
    annotations:
        argocd.argoproj.io/sync-wave: "-120"
    labels:
        app.kubernetes.io/name: cluster-bootstrap
    name: cluster-bootstrap
    namespace: cluster-bootstrap-system
spec:
    selector:
        matchLabels:
            app: cluster-bootstrap
    template:
        metadata:
            labels:
                app: cluster-bootstrap
        spec:
            containers:
                -   image: registry.k8s.io/pause:3.9
                    name: pause
                    resources:
                        {{- .Values.clusterBootstrap.resources | toYaml | nindent 24 }}
            initContainers:
                -   command:
                        -   sh
                        -   -c
                        -   |
                            {{- .Values.clusterBootstrap.script | nindent 28 }}
                    image: {{ .Values.clusterBootstrap.image }}
                    name: init-bootstraper
                    resources:
                        {{- .Values.clusterBootstrap.resources | toYaml | nindent 24 }}
                    securityContext:
                        privileged: true
                        runAsUser: 0
            nodeSelector:
                kubernetes.io/os: linux
---
apiVersion: v1
kind: Namespace
metadata:
    annotations:
        argocd.argoproj.io/sync-wave: "-120"
    labels:
        name: cluster-bootstrap-system
    name: cluster-bootstrap-system
{{- end -}}