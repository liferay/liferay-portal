<?xml version="1.0"?>

<workflow-definition
	xmlns="urn:liferay.com:liferay-workflow_7.4.0"
	xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xsi:schemaLocation="urn:liferay.com:liferay-workflow_7.4.0 http://www.liferay.com/dtd/liferay-workflow-definition_7_4_0.xsd"
>
	<name>Generate Tags</name>
	<version>1</version>
	<llm>
		<name>generateTags</name>
		<metadata>
			<![CDATA[
				{
					"xy": [
						276,
						242
					]
				}
			]]>
		</metadata>
		<labels>
			<label language-id="en_US">
				Generate Tags
			</label>
		</labels>
		<input-variables>
			<![CDATA[
				[
					{
						"name": "content",
						"type": "string"
					},
					{
						"name": "count",
						"type": "string"
					},
					{
						"name": "existingTags",
						"type": "string"
					}
				]
			]]>
		</input-variables>
		<output-variables>
			<![CDATA[
				[
					{
						"name": "suggestedTags",
						"type": "string"
					}
				]
			]]>
		</output-variables>
		<prompt>
			<![CDATA[# Role

You are a content tagging assistant. You receive an asset's text content and the list of tags that already exist in scope. You propose the tags that best describe the content, reusing existing tags wherever possible.

# Inputs

- content: the plain text body of the asset.
- existingTags: a JSON array string of tag names already available in scope.
- count: the maximum number of tags to return.

# Hard rules

- Prioritize reuse. Prefer tags from existingTags whenever they describe the content. Only propose a new tag when the existing tags do not adequately cover the content.
- Mark each suggestion with isNew: false when its name matches an existing tag (case insensitive), true otherwise.
- Return at most {{count}} suggestions; when count is empty or not provided, default to 3. Each suggestion carries a confidence between 0 and 1. Order suggestions by confidence, highest first. Drop weak matches rather than padding to {{count}}.

# Output policy

- Output a SINGLE raw JSON object and nothing else.
- DO NOT wrap the JSON in markdown fences (no ```json, no ```).
- DO NOT add prose, commentary, or trailing text.
- Shape:

{
	"suggestions": [
		{
			"confidence": <number between 0 and 1>,
			"isNew": <boolean>,
			"name": "<string, the tag>"
		}
	]
}]]>
		</prompt>
		<tools>
			<![CDATA[[]]]>
		</tools>
		<transitions>
			<transition>
				<labels>
					<label language-id="en_US">
						End
					</label>
				</labels>
				<name>end</name>
				<target>end</target>
				<default>true</default>
			</transition>
		</transitions>
		<user-message>
			<![CDATA[Content:
{{content}}

Existing tags (JSON):
{{existingTags}}

Number of tags requested: {{count}}]]>
		</user-message>
	</llm>
	<state>
		<name>start</name>
		<metadata>
			<![CDATA[
				{
					"xy": [
						273,
						25
					]
				}
			]]>
		</metadata>
		<initial>true</initial>
		<labels>
			<label language-id="en_US">
				Start
			</label>
		</labels>
		<transitions>
			<transition>
				<labels>
					<label language-id="en_US">
						Generate Tags
					</label>
				</labels>
				<name>generateTags</name>
				<target>generateTags</target>
				<default>true</default>
			</transition>
		</transitions>
	</state>
	<state>
		<name>end</name>
		<metadata>
			<![CDATA[
				{
					"terminal": true,
					"xy": [
						277,
						459
					]
				}
			]]>
		</metadata>
		<labels>
			<label language-id="en_US">
				End
			</label>
		</labels>
	</state>
</workflow-definition>